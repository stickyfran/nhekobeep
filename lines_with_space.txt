1:diff --git a/CMakeLists.txt b/CMakeLists.txt
2:index 8122313..0575fe9 100644
3:--- a/CMakeLists.txt
4:+++ b/CMakeLists.txt
5:@@ -463,8 +463,13 @@ set(SRC_FILES
6:     src/AvatarProvider.h
7:     src/BlurhashProvider.cpp
8:     src/BlurhashProvider.h
9:+    src/BeeperBridge.h
10:+    src/BeeperReinitController.cpp
11:+    src/BeeperReinitController.h
12:     src/Cache.cpp
13:     src/Cache.h
14:+    src/CacheRefreshController.cpp
15:+    src/CacheRefreshController.h
16:     src/CacheCryptoStructs.cpp
17:     src/CacheCryptoStructs.h
18:     src/CacheStructs.h
19:@@ -820,6 +825,8 @@ set(QML_SOURCES
20:     resources/qml/pages/RegisterPage.qml
21:     resources/qml/pages/UserSettingsPage.qml
22:     resources/qml/pages/WelcomePage.qml
23:+    resources/qml/ui/BeeperReinitOverlay.qml
24:+    resources/qml/ui/CacheRefreshOverlay.qml
25:     resources/qml/ui/NhekoSlider.qml
26:     resources/qml/ui/Ripple.qml
27:     resources/qml/ui/Snackbar.qml
28:diff --git a/resources/qml/Avatar.qml b/resources/qml/Avatar.qml
29:index 6217d70..7f61e2c 100644
30:--- a/resources/qml/Avatar.qml
31:+++ b/resources/qml/Avatar.qml
32:@@ -18,6 +18,11 @@ AbstractButton {
33:     property alias textColor: label.color
34:     property string url
35:     property string userid
36:+    // Outline color for Beeper bridge network (e.g. WhatsApp green)
37:+    readonly property color networkOutlineColor: (Settings.networkOutline && avatar.userid !== "") ? Rooms.networkColorForMxid(avatar.userid) : "transparent"
38:+
39:+    // Cache the effective pixel size to avoid recalculating on every render frame
40:+    property int effectivePixelSize: avatar.height * Screen.devicePixelRatio
41: 
42:     height: 48
43:     width: 48
44:@@ -27,6 +32,8 @@ AbstractButton {
45: 
46:         color: palette.alternateBase
47:         radius: Settings.avatarCircles ? height / 2 : height / 8
48:+        border.color: avatar.networkOutlineColor
49:+        border.width: avatar.networkOutlineColor.a > 0 ? 3 : 0
50:     }
51: 
52:     Label {
53:@@ -41,6 +48,8 @@ AbstractButton {
54:         textFormat: Text.RichText
55:         verticalAlignment: Text.AlignVCenter
56:         visible: img.status != Image.Ready && !Settings.useIdenticon
57:+
58:+        Behavior on opacity { NumberAnimation { duration: 120; easing.type: Easing.OutQuad } }
59:     }
60:     Image {
61:         id: identicon
62:@@ -48,6 +57,8 @@ AbstractButton {
63:         anchors.fill: parent
64:         source: Settings.useIdenticon ? ("image://jdenticon/" + (avatar.userid !== "" ? avatar.userid : avatar.roomid) + "?radius=" + (Settings.avatarCircles ? 100 : 25)) : ""
65:         visible: Settings.useIdenticon && img.status != Image.Ready
66:+
67:+        Behavior on opacity { NumberAnimation { duration: 120; easing.type: Easing.OutQuad } }
68:     }
69:     Image {
70:         id: img
71:@@ -64,16 +75,11 @@ AbstractButton {
72:         } else {
73:             return "";
74:         }
75:-        sourceSize.height: if (!avatar.url.startsWith('image://MxcImage/') && avatar.url.endsWith('.svg')){
76:-            return avatar.height
77:-        } else {
78:-            return avatar.height * Screen.devicePixelRatio
79:-        }
80:-        sourceSize.width: if (!avatar.url.startsWith('image://MxcImage/') && avatar.url.endsWith('.svg')){
81:-            return avatar.width
82:-        } else {
83:-            return avatar.width * Screen.devicePixelRatio
84:-        }
85:+        sourceSize.height: avatar.effectivePixelSize
86:+        sourceSize.width: avatar.effectivePixelSize
87:+        opacity: img.status == Image.Ready ? 1.0 : 0.0
88:+
89:+        Behavior on opacity { NumberAnimation { duration: 100 } }
90:     }
91:     Rectangle {
92:         id: onlineIndicator
93:@@ -86,7 +92,6 @@ AbstractButton {
94:                 return Nheko.theme.unavailable;
95:             case "offline":
96:             default:
97:-                // return "#a82353" don't show anything if offline, since it is confusing, if presence is disabled
98:                 return "transparent";
99:             }
100:         }
101:@@ -96,7 +101,7 @@ AbstractButton {
102:         color: updatePresence()
103:         height: avatar.height / 6
104:         radius: Settings.avatarCircles ? height / 2 : height / 8
105:-        visible: !!avatar.userid
106:+        visible: !!avatar.userid && !Settings.networkOutline
107:         width: height
108: 
109:         Connections {
110:diff --git a/resources/qml/CommunitiesList.qml b/resources/qml/CommunitiesList.qml
111:index a134474..6108850 100644
112:--- a/resources/qml/CommunitiesList.qml
113:+++ b/resources/qml/CommunitiesList.qml
114:@@ -29,12 +29,99 @@ Page {
115: 
116:         target: MainWindow
117:     }
118:+    header: Rectangle {
119:+        id: sidebarHeader
120:+        width: parent.width
121:+        height: collapsed ? 0 : 36
122:+        color: "transparent"
123:+        visible: !collapsed
124:+
125:+        RowLayout {
126:+            anchors.fill: parent
127:+            anchors.margins: Nheko.paddingSmall
128:+            spacing: Nheko.paddingSmall
129:+
130:+            Item {
131:+                Layout.fillWidth: true
132:+            }
133:+
134:+            ImageButton {
135:+                id: createSpaceBtn
136:+                Layout.preferredWidth: 22
137:+                Layout.preferredHeight: 22
138:+                image: ":/icons/icons/ui/plus.svg"
139:+                ToolTip.text: qsTr("Create Space")
140:+                ToolTip.visible: hovered
141:+                hoverEnabled: true
142:+
143:+                onClicked: createSpaceDialog.open()
144:+
145:+                Dialog {
146:+                    id: createSpaceDialog
147:+                    title: qsTr("Create Space")
148:+                    standardButtons: Dialog.Ok | Dialog.Cancel
149:+                    modal: true
150:+                    anchors.centerIn: parent
151:+                    width: 350
152:+
153:+                    ColumnLayout {
154:+                        spacing: Nheko.paddingMedium
155:+                        width: parent ? parent.width : 300
156:+
157:+                        Label { text: qsTr("Name:") }
158:+                        TextField {
159:+                            id: spaceNameField
160:+                            placeholderText: qsTr("My Space")
161:+                            Layout.fillWidth: true
162:+                        }
163:+
164:+                        Label { text: qsTr("Topic (optional):") }
165:+                        TextField {
166:+                            id: spaceTopicField
167:+                            placeholderText: qsTr("Description of this space")
168:+                            Layout.fillWidth: true
169:+                        }
170:+
171:+                        CheckBox {
172:+                            id: spacePublicCheck
173:+                            text: qsTr("Public space")
174:+                            checked: false
175:+                        }
176:+                    }
177:+
178:+                    onAccepted: {
179:+                        if (spaceNameField.text) {
180:+                            Communities.createSpace(
181:+                                spaceNameField.text,
182:+                                spaceTopicField.text,
183:+                                "",
184:+                                spacePublicCheck.checked);
185:+                        }
186:+                    }
187:+                }
188:+            }
189:+
190:+            ImageButton {
191:+                id: collapseBtn
192:+                Layout.preferredWidth: 22
193:+                Layout.preferredHeight: 22
194:+                image: ":/icons/icons/ui/collapse.svg"
195:+                ToolTip.text: qsTr("Collapse sidebar")
196:+                ToolTip.visible: hovered
197:+                hoverEnabled: true
198:+
199:+                onClicked: communitySidebar.collapsed = !communitySidebar.collapsed
200:+            }
201:+        }
202:+    }
203:+
204:     ListView {
205:         id: communitiesList
206: 
207:         anchors.left: parent.left
208:         anchors.right: parent.right
209:-        height: parent.height
210:+        anchors.top: sidebarHeader.bottom
211:+        height: parent.height - sidebarHeader.height
212:         model: Communities.filtered()
213:         boundsBehavior: Flickable.StopAtBounds
214: 
215:@@ -230,6 +317,77 @@ Page {
216: 
217:                 onTriggered: Communities.toggleTagId(communityContextMenu.tagId)
218:             }
219:+            MenuSeparator {}
220:+            MenuItem {
221:+                text: qsTr("Change icon...")
222:+                onTriggered: {
223:+                    var dialog = Qt.createComponent("qrc:/resources/qml/dialogs/IconPickerDialog.qml");
224:+                    if (dialog.status == Component.Ready) {
225:+                        var win = dialog.createObject(communitySidebar, {
226:+                            "currentTagId": communityContextMenu.tagId
227:+                        });
228:+                        win.show();
229:+                    }
230:+                }
231:+            
232:+                Component {
233:+                    id: editSpaceDialogComponent
234:+            
235:+                    Dialog {
236:+                        id: editSpaceDialog
237:+                        property string spaceId
238:+                        title: qsTr("Edit Space")
239:+                        standardButtons: Dialog.Ok | Dialog.Cancel
240:+                        modal: true
241:+                        anchors.centerIn: parent
242:+                        width: 350
243:+            
244:+                        ColumnLayout {
245:+                            spacing: Nheko.paddingMedium
246:+                            width: parent ? parent.width : 300
247:+            
248:+                            Label { text: qsTr("Name:") }
249:+                            TextField {
250:+                                id: editSpaceNameField
251:+                                placeholderText: qsTr("New space name")
252:+                                Layout.fillWidth: true
253:+                            }
254:+            
255:+                            Label { text: qsTr("Topic:") }
256:+                            TextField {
257:+                                id: editSpaceTopicField
258:+                                placeholderText: qsTr("New space topic")
259:+                                Layout.fillWidth: true
260:+                            }
261:+                        }
262:+            
263:+                        onAccepted: {
264:+                            Communities.updateSpace(
265:+                                editSpaceDialog.spaceId,
266:+                                editSpaceNameField.text,
267:+                                editSpaceTopicField.text,
268:+                                "");
269:+                        }
270:+                    }
271:+                }
272:+            }
273:+            MenuItem {
274:+                text: qsTr("Edit Space...")
275:+                enabled: communityContextMenu.tagId.startsWith("space:")
276:+                onTriggered: {
277:+                    var editDialog = editSpaceDialogComponent.createObject(communitySidebar, {
278:+                        "spaceId": communityContextMenu.tagId.toString().substring(6)
279:+                    });
280:+                    editDialog.show();
281:+                }
282:+            }
283:+            MenuItem {
284:+                text: qsTr("Delete Space")
285:+                enabled: communityContextMenu.tagId.startsWith("space:")
286:+                onTriggered: {
287:+                    Communities.deleteSpace(communityContextMenu.tagId.toString().substring(6));
288:+                }
289:+            }
290:         }
291:     }
292: }
293:diff --git a/resources/qml/RoomList.qml b/resources/qml/RoomList.qml
294:index db4f2cb..1bd3745 100644
295:--- a/resources/qml/RoomList.qml
296:+++ b/resources/qml/RoomList.qml
297:@@ -227,6 +227,20 @@ Page {
298:                 }
299:                 Item {
300:                 }
301:+                ImageButton {
302:+                    id: unreadSortToggle
303:+
304:+                    Layout.alignment: Qt.AlignVCenter
305:+                    Layout.preferredHeight: fontMetrics.lineSpacing * 1.5
306:+                    Layout.preferredWidth: fontMetrics.lineSpacing * 1.5
307:+                    ToolTip.delay: Nheko.tooltipDelay
308:+                    ToolTip.text: Settings.sortUnreadFirst ? qsTr("Unread first: ON") : qsTr("Unread first: OFF")
309:+                    ToolTip.visible: hovered
310:+                    image: Settings.sortUnreadFirst ? ":/icons/icons/ui/sort-down.svg" : ":/icons/icons/ui/sort.svg"
311:+                    visible: !collapsed
312:+
313:+                    onClicked: Settings.sortUnreadFirst = !Settings.sortUnreadFirst
314:+                }
315:                 ImageButton {
316:                     id: logoutButton
317: 
318:@@ -442,7 +456,10 @@ Page {
319:         model: Rooms
320:         boundsBehavior: Flickable.StopAtBounds
321: 
322:-        //reuseItems: true
323:+        reuseItems: true
324:+        // Pre-fetch delegates for items ~3 screens ahead in scroll direction
325:+        // so avatar images start loading before they come into view
326:+        cacheBuffer: avatarSize * 20
327:         ScrollBar.vertical: ScrollBar {
328:             id: scrollbar
329: 
330:@@ -467,6 +484,9 @@ Page {
331:             required property string roomId
332:             required property string roomName
333:             required property var tags
334:+            required property string beeperNetwork
335:+            required property color beeperNetworkColor
336:+            required property string beeperNetworkOtherUserId
337:             required property string time
338:             property color unimportantText: palette.buttonText
339: 
340:@@ -563,7 +583,7 @@ Page {
341:                     enabled: false
342:                     roomid: roomId
343:                     url: avatarUrl.replace("mxc://", "image://MxcImage/")
344:-                    userid: isDirect ? directChatOtherUserId : ""
345:+                    userid: isDirect ? (directChatOtherUserId || beeperNetworkOtherUserId) : beeperNetworkOtherUserId
346:                     Layout.preferredWidth: avatarSize
347:                     Layout.preferredHeight: avatarSize
348: 
349:@@ -602,10 +622,47 @@ Page {
350: 
351:                             anchors.left: parent.left
352:                             color: roomItem.importantText
353:-                            elideWidth: parent.width - (timestamp.visible ? timestamp.implicitWidth : 0) - (spaceNotificationBubble.visible ? spaceNotificationBubble.implicitWidth : 0)
354:+                            elideWidth: parent.width - (timestamp.visible ? timestamp.implicitWidth : 0) - (beeperBadge.visible ? beeperBadge.width + Nheko.paddingSmall : 0) - (spaceNotificationBubble.visible ? spaceNotificationBubble.implicitWidth : 0)
355:                             fullText: TimelineManager.htmlEscape(roomName)
356:                             textFormat: Text.RichText
357:                         }
358:+                        // Beeper bridge network badge with brand color
359:+                        Rectangle {
360:+                            id: beeperBadge
361:+
362:+                            readonly property color brandColor: beeperNetworkColor.a > 0 ? beeperNetworkColor : palette.highlight
363:+                            readonly property bool isLightColor: {
364:+                                if (beeperNetworkColor.a === 0)
365:+                                    return false;
366:+                                var c = beeperNetworkColor;
367:+                                var r = c.r;
368:+                                var g = c.g;
369:+                                var b = c.b;
370:+                                var luminance = 0.2126 * r + 0.7152 * g + 0.0722 * b;
371:+                                return luminance > 0.6;
372:+                            }
373:+
374:+                            anchors.left: titleText.right
375:+                            anchors.leftMargin: Nheko.paddingSmall
376:+                            anchors.verticalCenter: titleText.verticalCenter
377:+                            color: beeperNetworkColor.a > 0 ? Qt.lighter(brandColor, 1.4) : Qt.alpha(palette.highlight, 0.15)
378:+                            border.color: beeperNetworkColor.a > 0 ? brandColor : Qt.alpha(palette.highlight, 0.4)
379:+                            border.width: 1
380:+                            height: beeperLabel.implicitHeight + 4
381:+                            radius: 3
382:+                            visible: beeperNetwork !== "" && Settings.beeperNetworkBadge
383:+                            width: beeperLabel.implicitWidth + 8
384:+
385:+                            Text {
386:+                                id: beeperLabel
387:+
388:+                                anchors.centerIn: parent
389:+                                color: beeperBadge.isLightColor ? "#000000" : "#ffffff"
390:+                                font.bold: true
391:+                                font.pixelSize: fontMetrics.font.pixelSize * 0.65
392:+                                text: beeperNetwork
393:+                            }
394:+                        }
395:                         Label {
396:                             id: timestamp
397: 
398:@@ -793,6 +850,11 @@ Page {
399: 
400:                 onTriggered: Rooms.copyLink(roomContextMenu.roomid)
401:             }
402:+            MenuItem {
403:+                text: qsTr("Download chat history...")
404:+
405:+                onTriggered: TimelineManager.downloadHistoryForRoom(roomContextMenu.roomid)
406:+            }
407:             Menu {
408:                 id: tagsMenu
409: 
410:@@ -825,6 +887,31 @@ Page {
411:                     onObjectAdded: (index, object) => tagsMenu.insertItem(index, object)
412:                     onObjectRemoved: (index, object) => tagsMenu.removeItem(object)
413:                 }
414:+
415:+                MenuSeparator {
416:+                    visible: CustomLabelListModel.rowCount() > 0
417:+                }
418:+
419:+                Instantiator {
420:+                    model: CustomLabelListModel
421:+
422:+                    delegate: MenuItem {
423:+                        property string t: model.tag
424:+
425:+                        checkable: true
426:+                        checked: roomContextMenu.tags !== undefined && roomContextMenu.tags.includes(t)
427:+                        text: model.displayName
428:+
429:+                        onTriggered: Rooms.toggleTag(roomContextMenu.roomid, t, checked)
430:+                    }
431:+
432:+                    onObjectAdded: (index, object) => {
433:+                        var insertIdx = index + tagsMenu.count - 1;
434:+                        tagsMenu.insertItem(insertIdx, object);
435:+                    }
436:+                    onObjectRemoved: (index, object) => tagsMenu.removeItem(object)
437:+                }
438:+
439:                 MenuItem {
440:                     text: qsTr("Create new tag...")
441: 
442:diff --git a/resources/qml/pages/UserSettingsPage.qml b/resources/qml/pages/UserSettingsPage.qml
443:index 8f51e66..fb501a7 100644
444:--- a/resources/qml/pages/UserSettingsPage.qml
445:+++ b/resources/qml/pages/UserSettingsPage.qml
446:@@ -252,6 +252,39 @@ Rectangle {
447:                             }
448:                         }
449: 
450:+                        // --- Patch action buttons ---
451:+                        DelegateChoice {
452:+                            roleValue: UserSettingsModel.ForceCacheSyncAction
453:+                            Button {
454:+                                text: qsTr("FORCE SYNC")
455:+                                enabled: !CacheRefreshController.inProgress
456:+                                onClicked: {
457:+                                    CacheRefreshController.startCacheRefresh();
458:+                                }
459:+                            }
460:+                        }
461:+                        DelegateChoice {
462:+                            roleValue: UserSettingsModel.ForceReinitAction
463:+                            Button {
464:+                                text: qsTr("RE-INITIALIZE")
465:+                                onClicked: {
466:+                                    confirmReinitDialog.open();
467:+                                }
468:+                            }
469:+                        }
470:+                        DelegateChoice {
471:+                            roleValue: UserSettingsModel.ManageCustomLabelsAction
472:+                            Button {
473:+                                text: qsTr("MANAGE")
474:+                                onClicked: {
475:+                                    var dialog = customLabelsDialogComponent.createObject();
476:+                                    dialog.show();
477:+                                    destroyOnClose(dialog);
478:+                                }
479:+                            }
480:+                        }
481:+
482:+
483:                         DelegateChoice {
484:                             Text {
485:                                 text: model.value
486:@@ -263,6 +327,161 @@ Rectangle {
487:         }
488:     }
489: 
490:+    // --- Confirmation dialog for Force Re-Init (referenced from DelegateChooser) ---
491:+    Dialog {
492:+        id: confirmReinitDialog
493:+        title: qsTr("Confirm Full Re-Init")
494:+        standardButtons: Dialog.Ok | Dialog.Cancel
495:+        modal: true
496:+        anchors.centerIn: parent
497:+
498:+        ColumnLayout {
499:+            spacing: Nheko.paddingMedium
500:+            width: parent ? parent.width : 400
501:+
502:+            Label {
503:+                text: qsTr("Are you sure you want to perform a full Beeper re-initialization?")
504:+                font.bold: true
505:+                color: Nheko.theme.error
506:+                wrapMode: Text.WordWrap
507:+                Layout.fillWidth: true
508:+            }
509:+
510:+            Label {
511:+                text: qsTr("This will:\n\u2022 Delete all cached room data\n\u2022 Re-download all chat history\n\u2022 Re-fetch all avatars\n\nThe process may take several minutes.")
512:+                wrapMode: Text.WordWrap
513:+                Layout.fillWidth: true
514:+            }
515:+        }
516:+
517:+        onAccepted: {
518:+            BeeperReinitController.startBeeperReinit();
519:+        }
520:+    }
521:+
522:+    // --- Custom Labels management dialog (referenced from DelegateChooser) ---
523:+    Component {
524:+        id: customLabelsDialogComponent
525:+
526:+        Dialog {
527:+            id: customLabelsDialog
528:+            title: qsTr("Manage Custom Labels")
529:+            standardButtons: Dialog.Close
530:+            modal: true
531:+            anchors.centerIn: parent
532:+            width: Math.min(500, parent ? parent.width * 0.9 : 500)
533:+
534:+            ColumnLayout {
535:+                spacing: Nheko.paddingMedium
536:+                width: parent ? parent.width - Nheko.paddingLarge * 2 : 460
537:+
538:+                Label {
539:+                    text: qsTr("Define custom names and icons for tags.")
540:+                    wrapMode: Text.WordWrap
541:+                    Layout.fillWidth: true
542:+                }
543:+
544:+                ColumnLayout {
545:+                    id: customLabelsContainer
546:+                    objectName: "CustomBeeperLabels"
547:+                    Layout.fillWidth: true
548:+                    spacing: Nheko.paddingSmall
549:+
550:+                    Repeater {
551:+                        model: CustomLabelListModel
552:+
553:+                        delegate: RowLayout {
554:+                            Layout.fillWidth: true
555:+                            spacing: Nheko.paddingSmall
556:+
557:+                            TextField {
558:+                                Layout.fillWidth: true
559:+                                text: model.displayName
560:+                                placeholderText: qsTr("Display Name")
561:+                                onTextChanged: CustomLabelListModel.updateLabel(
562:+                                    index, model.tag, text, model.iconKey)
563:+                            }
564:+
565:+                            ComboBox {
566:+                                id: iconCombo
567:+                                Layout.preferredWidth: 120
568:+                                model: CustomLabelListModel.availableIcons()
569:+                                currentIndex: {
570:+                                    var icons = CustomLabelListModel.availableIcons();
571:+                                    return Math.max(0, icons.indexOf(model.iconKey));
572:+                                }
573:+                                onCurrentIndexChanged: {
574:+                                    if (currentIndex >= 0) {
575:+                                        var icons = CustomLabelListModel.availableIcons();
576:+                                        CustomLabelListModel.updateLabel(
577:+                                            index, model.tag, model.displayName,
578:+                                            icons[currentIndex]);
579:+                                    }
580:+                                }
581:+                            }
582:+
583:+                            Label {
584:+                                text: model.tag
585:+                                color: palette.buttonText
586:+                                font.pixelSize: fontMetrics.font.pixelSize * 0.85
587:+                                Layout.preferredWidth: 80
588:+                                elide: Text.ElideRight
589:+                            }
590:+
591:+                            ImageButton {
592:+                                Layout.preferredWidth: 22
593:+                                Layout.preferredHeight: 22
594:+                                image: ":/icons/icons/ui/delete.svg"
595:+                                ToolTip.text: qsTr("Remove label")
596:+                                ToolTip.visible: hovered
597:+                                hoverEnabled: true
598:+                                onClicked: CustomLabelListModel.removeLabel(index)
599:+                            }
600:+                        }
601:+                    }
602:+
603:+                    Button {
604:+                        text: qsTr("Add Custom Label")
605:+                        Layout.alignment: Qt.AlignLeft
606:+                        onClicked: addCustomLabelDialog.open()
607:+                    }
608:+                }
609:+            }
610:+
611:+            Dialog {
612:+                id: addCustomLabelDialog
613:+                title: qsTr("Add Custom Label")
614:+                standardButtons: Dialog.Ok | Dialog.Cancel
615:+                modal: true
616:+                anchors.centerIn: parent
617:+
618:+                ColumnLayout {
619:+                    spacing: Nheko.paddingMedium
620:+                    Label { text: qsTr("Tag:") }
621:+                    TextField {
622:+                        id: newTagField
623:+                        placeholderText: "u.mylabel"
624:+                        Layout.fillWidth: true
625:+                    }
626:+                    Label { text: qsTr("Display Name:") }
627:+                    TextField {
628:+                        id: newNameField
629:+                        placeholderText: qsTr("My Label")
630:+                        Layout.fillWidth: true
631:+                    }
632:+                }
633:+
634:+                onAccepted: {
635:+                    if (newTagField.text && newNameField.text) {
636:+                        CustomLabelListModel.addLabel(
637:+                            newTagField.text, newNameField.text,
638:+                            ":/icons/icons/ui/tag.svg");
639:+                    }
640:+                }
641:+            }
642:+        }
643:+    }
644:+
645:     ImageButton {
646:         id: backButton
647:         anchors.top: parent.top
648:@@ -275,5 +649,14 @@ Rectangle {
649:         ToolTip.text: qsTr("Back")
650:         onClicked: mainWindow.pop()
651:     }
652:+
653:+    // Overlay that blocks the UI during cache refresh.
654:+    CacheRefreshOverlay {
655:+        anchors.fill: parent
656:+    }
657:+
658:+    // Overlay that blocks the UI during Beeper re-initialization.
659:+    BeeperReinitOverlay {
660:+        anchors.fill: parent
661:+    }
662: }
663: 
664:diff --git a/src/AvatarProvider.cpp b/src/AvatarProvider.cpp
665:index 4608aca..e02f59a 100644
666:--- a/src/AvatarProvider.cpp
667:+++ b/src/AvatarProvider.cpp
668:@@ -68,6 +68,27 @@ resolve(const QString &room_id,
669: 
670:     resolve(std::move(avatarUrl), size, receiver, callback);
671: }
672:+
673:+// Dummy receiver used for pre-warming — lives long enough for async downloads
674:+static QObject *
675:+prewarmReceiver()
676:+{
677:+    static QObject *r = new QObject;
678:+    return r;
679:+}
680:+
681:+void
682:+prewarm(const QVector<QPair<QString, int>> &avatars)
683:+{
684:+    auto recv = prewarmReceiver();
685:+
686:+    for (const auto &[url, size] : avatars) {
687:+        resolve(url, size, recv, [](QPixmap) {
688:+            // Result discarded — only the cache population matters.
689:+            // The QPixmapCache insert happens inside resolve().
690:+        });
691:+    }
692:+}
693: }
694: 
695: #include "moc_AvatarProvider.cpp"
696:diff --git a/src/AvatarProvider.h b/src/AvatarProvider.h
697:index b73b1ad..187c540 100644
698:--- a/src/AvatarProvider.h
699:+++ b/src/AvatarProvider.h
700:@@ -28,4 +28,11 @@ resolve(const QString &room_id,
701:         int size,
702:         QObject *receiver,
703:         AvatarCallback cb);
704:+
705:+// Pre-warm the avatar cache for a list of (avatarUrl, size) pairs.
706:+// This triggers asynchronous downloads into both the disk cache and
707:+// the QPixmapCache (VRAM), so avatars appear instantly when scrolled to.
708:+// The download responses are discarded — only the cache population matters.
709:+void
710:+prewarm(const QVector<QPair<QString, int>> &avatars);
711: }
712:diff --git a/src/Cache.cpp b/src/Cache.cpp
713:index 0f621df..ba1d1b9 100644
714:--- a/src/Cache.cpp
715:+++ b/src/Cache.cpp
716:@@ -282,6 +282,36 @@ Cache::getMembersDb(lmdb::txn &txn, const std::string &room_id)
717:     return lmdb::dbi::open(txn, std::string(room_id + "/members").c_str(), MDB_CREATE);
718: }
719: 
720:+lmdb::env &
721:+Cache::env()
722:+{
723:+    return db->env_;
724:+}
725:+
726:+lmdb::dbi
727:+Cache::roomsDb()
728:+{
729:+    return db->rooms;
730:+}
731:+
732:+lmdb::dbi
733:+Cache::openMembersDb(lmdb::txn &txn, const std::string &room_id)
734:+{
735:+    return getMembersDb(txn, room_id);
736:+}
737:+
738:+lmdb::dbi
739:+Cache::openStatesDb(lmdb::txn &txn, const std::string &room_id)
740:+{
741:+    return getStatesDb(txn, room_id);
742:+}
743:+
744:+const QString &
745:+Cache::localUserId() const
746:+{
747:+    return localUserId_;
748:+}
749:+
750: lmdb::dbi
751: Cache::getUserKeysDb(lmdb::txn &txn)
752: {
753:@@ -2566,8 +2596,12 @@ try {
754:             }
755:         }
756: 
757:+        // Only update the modification timestamp from actual message events,
758:+        // NOT from state events (e.g. avatar/name changes, member joins).
759:+        // This ensures rooms are sorted chronologically by their last real message,
760:+        // not by an avatar change or similar state event.
761:         for (const auto &e : room.second.timeline.events) {
762:-            if (!mtx::accessors::is_message(e))
763:+            if (!mtx::accessors::is_message(e) || mtx::accessors::is_state_event(e))
764:                 continue;
765:             updatedInfo.approximate_last_modification_ts = mtx::accessors::origin_server_ts_ms(e);
766:         }
767:@@ -3360,6 +3394,79 @@ Cache::invite(std::string_view roomid)
768:     return result;
769: }
770: 
771:+namespace {
772:+//
773:+// Beeper "fake DM" detection helpers.
774:+//
775:+// Beeper bridges (Instagram, WhatsApp, etc.) model 1:1 chats as 3-member
776:+// rooms: <local user, real contact, @<service>bot:beeper.<tld>>. These
777:+// helpers identify the bridge bot and the real counterpart so we can
778:+// render the room as a native Matrix DM instead of a tiny group.
779:+//
780:+bool
781:+isBeeperBridgeBotMxid(std::string_view mxid)
782:+{
783:+    if (mxid.size() < 4 || mxid.front() != '@')
784:+        return false;
785:+
786:+    const auto colon = mxid.find(':');
787:+    if (colon == std::string_view::npos || colon + 1 >= mxid.size())
788:+        return false;
789:+
790:+    const auto localpart = mxid.substr(1, colon - 1);
791:+    const auto server    = mxid.substr(colon + 1);
792:+
793:+    // server must start with "beeper." (e.g. beeper.local, beeper.com)
794:+    if (server.size() < 7 || server.compare(0, 7, "beeper.") != 0)
795:+        return false;
796:+
797:+    // localpart must contain "bot" (case-insensitive)
798:+    auto toLower = [](char c) -> char {
799:+        return (c >= 'A' && c <= 'Z') ? static_cast<char>(c + ('a' - 'A')) : c;
800:+    };
801:+    for (size_t i = 0; i + 3 <= localpart.size(); ++i) {
802:+        if (toLower(localpart[i]) == 'b' && toLower(localpart[i + 1]) == 'o' &&
803:+            toLower(localpart[i + 2]) == 't')
804:+            return true;
805:+    }
806:+    return false;
807:+}
808:+
809:+//
810:+// Given a fully-populated <mxid -> MemberInfo> map (containing exactly
811:+// the three members of a candidate Beeper room), returns a pointer to
812:+// the real counterpart's MemberInfo when the room is a Beeper "fake DM".
813:+// Returns nullptr otherwise.
814:+//
815:+const MemberInfo *
816:+beeperFakeDmCounterpart(const std::map<std::string, MemberInfo> &members,
817:+                        const std::string &localUserId)
818:+{
819:+    if (members.size() != 3)
820:+        return nullptr;
821:+
822:+    bool hasLocal              = false;
823:+    bool hasBot                = false;
824:+    const MemberInfo *contact  = nullptr;
825:+
826:+    for (const auto &[mxid, info] : members) {
827:+        if (mxid == localUserId) {
828:+            hasLocal = true;
829:+        } else if (isBeeperBridgeBotMxid(mxid)) {
830:+            hasBot = true;
831:+        } else {
832:+            // Only accept a single non-local, non-bot member as the
833:+            // counterpart; otherwise this isn't a 1:1-shaped room.
834:+            if (contact)
835:+                return nullptr;
836:+            contact = &info;
837:+        }
838:+    }
839:+
840:+    return (hasLocal && hasBot) ? contact : nullptr;
841:+}
842:+} // namespace
843:+
844: QString
845: Cache::getRoomAvatarUrl(lmdb::txn &txn, lmdb::dbi &statesdb, lmdb::dbi &membersdb)
846: {
847:@@ -3382,6 +3489,50 @@ Cache::getRoomAvatarUrl(lmdb::txn &txn, lmdb::dbi &statesdb, lmdb::dbi &membersd
848:         }
849:     }
850: 
851:+    // Beeper "fake DM": prefer the real contact's individual avatar over
852:+    // the (usually empty) group avatar Nheko would otherwise compose.
853:+    if (membersdb.size(txn) == 3) {
854:+        std::map<std::string, MemberInfo> members;
855:+        auto bcursor = lmdb::cursor::open(txn, membersdb);
856:+        std::string_view buser_id, bmember_data;
857:+        while (bcursor.get(buser_id, bmember_data, MDB_NEXT)) {
858:+            try {
859:+                members.emplace(buser_id,
860:+                                nlohmann::json::parse(bmember_data).get<MemberInfo>());
861:+            } catch (const nlohmann::json::exception &e) {
862:+                nhlog::db()->warn("failed to parse member info: {}", e.what());
863:+            }
864:+        }
865:+        bcursor.close();
866:+
867:+        if (const auto *cp = beeperFakeDmCounterpart(members, localUserId_.toStdString())) {
868:+            if (!cp->avatar_url.empty())
869:+                return QString::fromStdString(cp->avatar_url);
870:+        }
871:+    }
872:+
873:     // We don't use an avatar for group chats.
874:     if (membersdb.size(txn) > 2)
875:         return QString();
876:@@ -3473,6 +3624,17 @@ Cache::getRoomName(lmdb::txn &txn, lmdb::dbi &statesdb, lmdb::dbi &membersdb)
877:     if (total == 1 && !members.empty())
878:         return QString::fromStdString(members.begin()->second.name);
879: 
880:+    // Beeper "fake DM": render as a 1:1 with the real contact's name only,
881:+    // hiding the bridge bot ("@<service>bot:beeper.<tld>"). This avoids
882:+    // localised strings like "Juan Pérez y Instagram Bridge bot" leaking
883:+    // into the room list.
884:+    if (total == 3) {
885:+        if (const auto *cp = beeperFakeDmCounterpart(members, localUserId_.toStdString())) {
886:+            if (!cp->name.empty())
887:+                return QString::fromStdString(cp->name);
888:+        }
889:+    }
890:+
891:     auto first_member = [&members, this]() {
892:         for (const auto &m : members) {
893:             if (m.first != localUserId_.toStdString())
894:diff --git a/src/Cache_p.h b/src/Cache_p.h
895:index 89fdd84..8f01e92 100644
896:--- a/src/Cache_p.h
897:+++ b/src/Cache_p.h
898:@@ -9,6 +9,8 @@
899: #include <QDateTime>
900: #include <QString>
901: 
902:+#include <lmdb++.h>
903:+
904: #include <mtx/events/collections.hpp>
905: #include <mtx/responses/notifications.hpp>
906: #include <mtx/responses/sync.hpp>
907:@@ -36,6 +38,22 @@ class Cache final : public QObject
908: 
909: public:
910:     Cache(const QString &userId, QObject *parent = nullptr);
911:+
912:+    //! Allow CacheRefreshController to perform surgical LMDB updates.
913:+    friend class CacheRefreshController;
914:+    //! Allow BeeperReinitWorker to reset the next_batch token and drop databases.
915:+    friend class BeeperReinitWorker;
916:+
917:+    //! Return the underlying LMDB environment for direct access.
918:+    lmdb::env &env();
919:+    //! Return the rooms database handle.
920:+    lmdb::dbi roomsDb();
921:+    //! Open the members database for a given room.
922:+    lmdb::dbi openMembersDb(lmdb::txn &txn, const std::string &room_id);
923:+    //! Open the states database for a given room.
924:+    lmdb::dbi openStatesDb(lmdb::txn &txn, const std::string &room_id);
925:+    //! The local user's Matrix ID.
926:+    const QString &localUserId() const;
927:     ~Cache() noexcept;
928: 
929:     std::string displayName(const std::string &room_id, const std::string &user_id);
930:diff --git a/src/ChatPage.h b/src/ChatPage.h
931:index d89457d..6a5f9d9 100644
932:--- a/src/ChatPage.h
933:+++ b/src/ChatPage.h
934:@@ -220,4 +220,6 @@ private:
935: 
936:     // Stores when our windows lost focus. Invalid when our windows have focus.
937:     QDateTime lastWindowActive;
938:+    //! Allow BeeperReinitController to pause/resume the sync loop.
939:+    friend class BeeperReinitController;
940: };
941:diff --git a/src/EventAccessors.cpp b/src/EventAccessors.cpp
942:index a43d62c..3dfeaaa 100644
943:--- a/src/EventAccessors.cpp
944:+++ b/src/EventAccessors.cpp
945:@@ -363,6 +363,27 @@ mtx::accessors::msg_type(const mtx::events::collections::TimelineEvents &event)
946:     return std::visit(EventMsgType{}, event);
947: }
948: 
949:+namespace {
950:+struct EventMsgTypeStr
951:+{
952:+    template<class T>
953:+    std::string operator()(const mtx::events::Event<T> &e)
954:+    {
955:+        if constexpr (requires(decltype(e) t) { t.content.msgtype.value(); })
956:+            return e.content.msgtype.value();
957:+        else if constexpr (requires(decltype(e) t) { std::string{t.content.msgtype}; })
958:+            return e.content.msgtype;
959:+        return "";
960:+    }
961:+};
962:+}
963:+
964:+std::string
965:+mtx::accessors::msg_type_str(const mtx::events::collections::TimelineEvents &event)
966:+{
967:+    return std::visit(EventMsgTypeStr{}, event);
968:+}
969:+
970: std::string
971: mtx::accessors::room_name(const mtx::events::collections::TimelineEvents &event)
972: {
973:diff --git a/src/EventAccessors.h b/src/EventAccessors.h
974:index 9868d29..890fe07 100644
975:--- a/src/EventAccessors.h
976:+++ b/src/EventAccessors.h
977:@@ -45,6 +45,8 @@ mtx::events::EventType
978: event_type(const mtx::events::collections::TimelineEvents &event);
979: mtx::events::MessageType
980: msg_type(const mtx::events::collections::TimelineEvents &event);
981:+std::string
982:+msg_type_str(const mtx::events::collections::TimelineEvents &event);
983: bool
984: is_message(const mtx::events::collections::TimelineEvents &event);
985: std::string
986:diff --git a/src/MxcImageProvider.cpp b/src/MxcImageProvider.cpp
987:index ba4c4dd..3fd37ea 100644
988:--- a/src/MxcImageProvider.cpp
989:+++ b/src/MxcImageProvider.cpp
990:@@ -195,13 +195,13 @@ MxcImageProvider::download(const QString &id,
991:         // Protect against synapse not following the spec:
992:         // https://github.com/matrix-org/synapse/issues/5302
993:         && requestedSize.height() <= 600 && requestedSize.width() <= 800) {
994:-        QString fileName = QStringLiteral("%1_%2x%3_%4_radius%5")
995:-                             .arg(QString::fromUtf8(id.toUtf8().toBase64(
996:-                               QByteArray::Base64UrlEncoding | QByteArray::OmitTrailingEquals)))
997:-                             .arg(requestedSize.width())
998:-                             .arg(requestedSize.height())
999:-                             .arg(crop ? "crop" : "scale")
1000:-                             .arg(radius);
1001:+        QString fileName = QStringLiteral("%1_%2x%3_%4_radius%5.webp")
1002:+                           .arg(QString::fromUtf8(id.toUtf8().toBase64(
1003:+                             QByteArray::Base64UrlEncoding | QByteArray::OmitTrailingEquals)))
1004:+                           .arg(requestedSize.width())
1005:+                           .arg(requestedSize.height())
1006:+                           .arg(crop ? "crop" : "scale")
1007:+                           .arg(radius);
1008:         QFileInfo fileInfo(QStandardPaths::writableLocation(QStandardPaths::CacheLocation) +
1009:                              "/media_cache",
1010:                            fileName);
1011:@@ -279,7 +279,7 @@ MxcImageProvider::download(const QString &id,
1012:                   }
1013:               }
1014:               image.setText(QStringLiteral("mxc url"), "mxc://" + id);
1015:-              if (image.save(fileInfo.absoluteFilePath(), "png")) {
1016:+              if (image.save(fileInfo.absoluteFilePath(), "webp")) {
1017:                   utils::markFileAsFromWeb(fileInfo.absoluteFilePath());
1018:                   nhlog::ui()->debug("Wrote: {}", fileInfo.absoluteFilePath().toStdString());
1019:               } else
1020:@@ -290,10 +290,10 @@ MxcImageProvider::download(const QString &id,
1021:           });
1022:     } else {
1023:         try {
1024:-            QString fileName = QStringLiteral("%1_radius%2")
1025:-                                 .arg(QString::fromUtf8(id.toUtf8().toBase64(
1026:-                                   QByteArray::Base64UrlEncoding | QByteArray::OmitTrailingEquals)))
1027:-                                 .arg(radius);
1028:+            QString fileName = QStringLiteral("%1_radius%2.webp")
1029:+                               .arg(QString::fromUtf8(id.toUtf8().toBase64(
1030:+                                 QByteArray::Base64UrlEncoding | QByteArray::OmitTrailingEquals)))
1031:+                               .arg(radius);
1032: 
1033:             QFileInfo fileInfo(QStandardPaths::writableLocation(QStandardPaths::CacheLocation) +
1034:                                  "/media_cache",
1035:diff --git a/src/UserSettingsPage.cpp b/src/UserSettingsPage.cpp
1036:index db3bf7f..f607fba 100644
1037:--- a/src/UserSettingsPage.cpp
1038:+++ b/src/UserSettingsPage.cpp
1039:@@ -92,12 +92,15 @@ UserSettings::load(std::optional<QString> profile)
1040:     typingNotifications_  = settings.value("user/typing_notifications", true).toBool();
1041:     sortByImportance_     = settings.value("user/sort_by_unread", true).toBool();
1042:     sortByAlphabet_       = settings.value("user/sort_by_alphabet", false).toBool();
1043:+    sortUnreadFirst_      = settings.value("user/sort_unread_first", false).toBool();
1044:     readReceipts_         = settings.value("user/read_receipts", true).toBool();
1045:     theme_                = settings.value("user/theme", defaultTheme_).toString();
1046: 
1047:     font_ = settings.value("user/font_family", "").toString();
1048: 
1049:     avatarCircles_        = settings.value("user/avatar_circles", true).toBool();
1050:+    networkOutline_       = settings.value("user/network_outline", true).toBool();
1051:+    beeperNetworkBadge_   = settings.value("user/beeper_network_badge", true).toBool();
1052:     useIdenticon_         = settings.value("user/use_identicon", true).toBool();
1053:     openImageExternal_    = settings.value("user/open_image_external", false).toBool();
1054:     openVideoExternal_    = settings.value("user/open_video_external", false).toBool();
1055:@@ -172,6 +175,8 @@ UserSettings::load(std::optional<QString> profile)
1056:     disableCertificateValidation_ =
1057:       settings.value("disable_certificate_validation", false).toBool();
1058: 
1059:+    loadCustomLabels();
1060:+
1061:     applyTheme();
1062: 
1063:     if (profile)
1064:@@ -432,6 +437,26 @@ UserSettings::setSortByAlphabet(bool state)
1065:     save();
1066: }
1067: 
1068:+void
1069:+UserSettings::setSortUnreadFirst(bool state)
1070:+{
1071:+    if (state == sortUnreadFirst_)
1072:+        return;
1073:+    sortUnreadFirst_ = state;
1074:+    emit sortUnreadFirstChanged(state);
1075:+    save();
1076:+}
1077:+
1078: void
1079: UserSettings::setButtonsInTimeline(bool state)
1080: {
1081:@@ -500,6 +525,46 @@ UserSettings::setAvatarCircles(bool state)
1082:     save();
1083: }
1084: 
1085:+void
1086:+UserSettings::setNetworkOutline(bool state)
1087:+{
1088:+    if (state == networkOutline_)
1089:+        return;
1090:+    networkOutline_ = state;
1091:+    emit networkOutlineChanged(state);
1092:+    save();
1093:+}
1094:+
1095:+void
1096:+UserSettings::setBeeperNetworkBadge(bool state)
1097:+{
1098:+    if (state == beeperNetworkBadge_)
1099:+        return;
1100:+    beeperNetworkBadge_ = state;
1101:+    emit beeperNetworkBadgeChanged(state);
1102:+    save();
1103:+}
1104:+
1105: void
1106: UserSettings::setDecryptSidebar(bool state)
1107: {
1108:@@ -930,6 +995,8 @@ UserSettings::save()
1109:     settings.endGroup(); // timeline
1110: 
1111:     settings.setValue("avatar_circles", avatarCircles_);
1112:+    settings.setValue("network_outline", networkOutline_);
1113:+    settings.setValue("beeper_network_badge", beeperNetworkBadge_);
1114:     settings.setValue("decrypt_sidebar", decryptSidebar_);
1115:     settings.setValue("decrypt_notifications", decryptNotifications_);
1116:     settings.setValue("space_notifications", spaceNotifications_);
1117:@@ -1013,6 +1080,228 @@ UserSettings::save()
1118:     settings.sync();
1119: }
1120: 
1121:+// -----------------------------------------------------------------------
1122:+// Beeper Custom Label persistence
1123:+// -----------------------------------------------------------------------
1124:+void
1125:+UserSettings::loadCustomLabels()
1126:+{
1127:+    customLabels_.clear();
1128:+    auto raw = settings.value("user/beeper_custom_labels").toList();
1129:+    for (const auto &item : std::as_const(raw)) {
1130:+        auto map = item.toMap();
1131:+        CustomLabel label;
1132:+        label.tag         = map.value("tag").toString();
1133:+        label.displayName = map.value("displayName").toString();
1134:+        label.iconKey     = map.value("iconKey").toString();
1135:+        if (!label.tag.isEmpty() && !label.displayName.isEmpty())
1136:+            customLabels_.append(QVariant::fromValue(label));
1137:+    }
1138:+}
1139:+
1140:+void
1141:+UserSettings::addCustomLabel(const QString &tag,
1142:+                              const QString &displayName,
1143:+                              const QString &iconKey)
1144:+{
1145:+    for (int i = 0; i < customLabels_.size(); ++i) {
1146:+        auto label = customLabels_[i].value<CustomLabel>();
1147:+        if (label.tag == tag) {
1148:+            customLabels_[i] = QVariant::fromValue(CustomLabel{tag, displayName, iconKey});
1149:+            saveCustomLabels();
1150:+            emit customLabelsChanged();
1151:+            return;
1152:+        }
1153:+    }
1154:+    customLabels_.append(QVariant::fromValue(CustomLabel{tag, displayName, iconKey}));
1155:+    saveCustomLabels();
1156:+    emit customLabelsChanged();
1157:+}
1158:+
1159:+void
1160:+UserSettings::removeCustomLabel(const QString &tag)
1161:+{
1162:+    customLabels_.erase(std::remove_if(customLabels_.begin(),
1163:+                                        customLabels_.end(),
1164:+                                        [&tag](const QVariant &v) {
1165:+                                            return v.value<CustomLabel>().tag == tag;
1166:+                                        }),
1167:+                         customLabels_.end());
1168:+    saveCustomLabels();
1169:+    emit customLabelsChanged();
1170:+}
1171:+
1172:+void
1173:+UserSettings::updateCustomLabel(const QString &tag,
1174:+                                 const QString &displayName,
1175:+                                 const QString &iconKey)
1176:+{
1177:+    for (auto &v : customLabels_) {
1178:+        auto label = v.value<CustomLabel>();
1179:+        if (label.tag == tag) {
1180:+            v = QVariant::fromValue(CustomLabel{tag, displayName, iconKey});
1181:+            saveCustomLabels();
1182:+            emit customLabelsChanged();
1183:+            return;
1184:+        }
1185:+    }
1186:+    addCustomLabel(tag, displayName, iconKey);
1187:+}
1188:+
1189:+QString
1190:+UserSettings::customLabelDisplayName(const QString &rawTag) const
1191:+{
1192:+    for (const auto &v : customLabels_) {
1193:+        auto label = v.value<CustomLabel>();
1194:+        if (label.tag == rawTag)
1195:+            return label.displayName;
1196:+    }
1197:+    return {};
1198:+}
1199:+
1200:+QString
1201:+UserSettings::customLabelIconKey(const QString &rawTag) const
1202:+{
1203:+    for (const auto &v : customLabels_) {
1204:+        auto label = v.value<CustomLabel>();
1205:+        if (label.tag == rawTag)
1206:+            return label.iconKey;
1207:+    }
1208:+    return {};
1209:+}
1210:+
1211:+void
1212:+UserSettings::saveCustomLabels()
1213:+{
1214:+    QVariantList raw;
1215:+    raw.reserve(customLabels_.size());
1216:+    for (const auto &v : std::as_const(customLabels_)) {
1217:+        auto label = v.value<CustomLabel>();
1218:+        QVariantMap map;
1219:+        map["tag"]         = label.tag;
1220:+        map["displayName"] = label.displayName;
1221:+        map["iconKey"]     = label.iconKey;
1222:+        raw.append(map);
1223:+    }
1224:+    settings.setValue("user/beeper_custom_labels", raw);
1225:+}
1226:+
1227:+// -----------------------------------------------------------------------
1228:+// CustomLabelListModel
1229:+// -----------------------------------------------------------------------
1230:+CustomLabelListModel::CustomLabelListModel(QObject *parent)
1231:+  : QAbstractListModel(parent)
1232:+{
1233:+    instance_ = this;
1234:+    connect(UserSettings::instance().get(),
1235:+            &UserSettings::customLabelsChanged,
1236:+            this,
1237:+            &CustomLabelListModel::refreshFromSettings);
1238:+    refreshFromSettings();
1239:+}
1240:+
1241:+CustomLabelListModel *
1242:+CustomLabelListModel::create(QQmlEngine *, QJSEngine *)
1243:+{
1244:+    Q_ASSERT(instance_);
1245:+    QJSEngine::setObjectOwnership(instance_, QJSEngine::CppOwnership);
1246:+    return instance_;
1247:+}
1248:+
1249:+QHash<int, QByteArray>
1250:+CustomLabelListModel::roleNames() const
1251:+{
1252:+    return {
1253:+      {TagRole, "tag"},
1254:+      {DisplayNameRole, "displayName"},
1255:+      {IconKeyRole, "iconKey"},
1256:+    };
1257:+}
1258:+
1259:+QVariant
1260:+CustomLabelListModel::data(const QModelIndex &index, int role) const
1261:+{
1262:+    if (index.row() < 0 || index.row() >= labels_.size())
1263:+        return {};
1264:+
1265:+    const auto &label = labels_.at(index.row());
1266:+    switch (role) {
1267:+    case TagRole:
1268:+        return label.tag;
1269:+    case DisplayNameRole:
1270:+        return label.displayName;
1271:+    case IconKeyRole:
1272:+        return label.iconKey;
1273:+    default:
1274:+        return {};
1275:+    }
1276:+}
1277:+
1278:+void
1279:+CustomLabelListModel::addLabel(const QString &tag,
1280:+                                const QString &displayName,
1281:+                                const QString &iconKey)
1282:+{
1283:+    UserSettings::instance()->addCustomLabel(tag, displayName, iconKey);
1284:+}
1285:+
1286:+void
1287:+CustomLabelListModel::removeLabel(int row)
1288:+{
1289:+    if (row >= 0 && row < labels_.size())
1290:+        UserSettings::instance()->removeCustomLabel(labels_.at(row).tag);
1291:+}
1292:+
1293:+void
1294:+CustomLabelListModel::updateLabel(int row,
1295:+                                   const QString &tag,
1296:+                                   const QString &displayName,
1297:+                                   const QString &iconKey)
1298:+{
1299:+    if (row >= 0 && row < labels_.size())
1300:+        UserSettings::instance()->updateCustomLabel(tag, displayName, iconKey);
1301:+}
1302:+
1303:+QStringList
1304:+CustomLabelListModel::availableIcons() const
1305:+{
1306:+    return {
1307:+      QStringLiteral(":/icons/icons/ui/tag.svg"),
1308:+      QStringLiteral(":/icons/icons/ui/star.svg"),
1309:+      QStringLiteral(":/icons/icons/ui/bookmark.svg"),
1310:+      QStringLiteral(":/icons/icons/ui/chat.svg"),
1311:+      QStringLiteral(":/icons/icons/ui/clock.svg"),
1312:+      QStringLiteral(":/icons/icons/ui/building-shop.svg"),
1313:+      QStringLiteral(":/icons/icons/ui/people.svg"),
1314:+      QStringLiteral(":/icons/icons/ui/world.svg"),
1315:+      QStringLiteral(":/icons/icons/ui/alert.svg"),
1316:+      QStringLiteral(":/icons/icons/ui/checkmark.svg"),
1317:+    };
1318:+}
1319:+
1320:+QString
1321:+CustomLabelListModel::displayNameForTag(const QString &tag) const
1322:+{
1323:+    return UserSettings::instance()->customLabelDisplayName(tag);
1324:+}
1325:+
1326:+QString
1327:+CustomLabelListModel::iconKeyForTag(const QString &tag) const
1328:+{
1329:+    return UserSettings::instance()->customLabelIconKey(tag);
1330:+}
1331:+
1332:+void
1333:+CustomLabelListModel::refreshFromSettings()
1334:+{
1335:+    beginResetModel();
1336:+    labels_.clear();
1337:+    auto custom = UserSettings::instance()->customLabels();
1338:+    for (const auto &v : std::as_const(custom))
1339:+        labels_.append(v.value<CustomLabel>());
1340:+    endResetModel();
1341:+}
1342:+
1343: QHash<int, QByteArray>
1344: UserSettingsModel::roleNames() const
1345: {
1346:@@ -1196,6 +1485,18 @@ UserSettingsModel::data(const QModelIndex &index, int role) const
1347:             return tr("Periodically update community routing information");
1348:         case ExpireEvents:
1349:             return tr("Periodically delete expired events");
1350:+        case PatchesSection:
1351:+            return tr("PARCHES");
1352:+        case NetworkOutline:
1353:+            return tr("Network outline on avatars");
1354:+        case BeeperNetworkBadge:
1355:+            return tr("Beeper network badge");
1356:+        case ForceCacheSync:
1357:+            return tr("Force Cache Sync");
1358:+        case ForceReinit:
1359:+            return tr("Force Full Re-Init");
1360:+        case ManageCustomLabels:
1361:+            return tr("Manage Custom Labels");
1362:         }
1363:     } else if (role == Value) {
1364:         switch (index.row()) {
1365:@@ -1342,6 +1643,14 @@ UserSettingsModel::data(const QModelIndex &index, int role) const
1366:             return i->updateSpaceVias();
1367:         case ExpireEvents:
1368:             return i->expireEvents();
1369:+        case NetworkOutline:
1370:+            return i->networkOutline();
1371:+        case BeeperNetworkBadge:
1372:+            return i->beeperNetworkBadge();
1373:+        case ForceCacheSync:
1374:+        case ForceReinit:
1375:+        case ManageCustomLabels:
1376:+            return {};
1377:         }
1378:     } else if (role == Description) {
1379:         switch (index.row()) {
1380:@@ -1437,6 +1746,10 @@ UserSettingsModel::data(const QModelIndex &index, int role) const
1381:         case AvatarCircles:
1382:             return tr(
1383:               "Change the appearance of user avatars in chats.\nOFF - square, ON - circle.");
1384:+        case BeeperNetworkBadge:
1385:+            return tr("Show a badge with the network name (e.g. WhatsApp) next to the room name.");
1386:+        case NetworkOutline:
1387:+            return tr("Show a colored outline on avatars based on the user's bridge network (e.g. WhatsApp green, Telegram blue).");
1388:         case UseIdenticon:
1389:             return tr("Display an identicon instead of a letter when no avatar is set.");
1390:         case OpenImageExternal:
1391:@@ -1543,6 +1856,12 @@ UserSettingsModel::data(const QModelIndex &index, int role) const
1392:                       "to have one client running this regularly.");
1393:         case IgnoredUsers:
1394:             return tr("Manage your ignored users.");
1395:+        case ForceCacheSync:
1396:+            return tr("If contacts or rooms appear with incorrect names or avatars, use this to force a refresh of their profile data.");
1397:+        case ForceReinit:
1398:+            return tr("Warning: This will delete all cached room data and rebuild from scratch. Use only if chat names or avatars are incorrect.");
1399:+        case ManageCustomLabels:
1400:+            return tr("Define custom names and icons for tags.");
1401:         }
1402:     } else if (role == Type) {
1403:         switch (index.row()) {
1404:@@ -1633,6 +1952,18 @@ UserSettingsModel::data(const QModelIndex &index, int role) const
1405:             return ConfigureHiddenEvents;
1406:         case IgnoredUsers:
1407:             return ManageIgnoredUsers;
1408:+        case PatchesSection:
1409:+            return SectionTitle;
1410:+        case NetworkOutline:
1411:+            return Toggle;
1412:+        case BeeperNetworkBadge:
1413:+            return Toggle;
1414:+        case ForceCacheSync:
1415:+            return ForceCacheSyncAction;
1416:+        case ForceReinit:
1417:+            return ForceReinitAction;
1418:+        case ManageCustomLabels:
1419:+            return ManageCustomLabelsAction;
1420:         }
1421:     } else if (role == ValueLowerBound) {
1422:         switch (index.row()) {
1423:@@ -1928,6 +2259,34 @@ UserSettingsModel::setData(const QModelIndex &index, const QVariant &value, int
1424:             } else
1425:                 return false;
1426:         }
1427:+        case NetworkOutline: {
1428:+            if (value.userType() == QMetaType::Bool) {
1429:+                i->setNetworkOutline(value.toBool());
1430:+                return true;
1431:+            } else
1432:+                return false;
1433:+        }
1434:+        case BeeperNetworkBadge: {
1435:+            if (value.userType() == QMetaType::Bool) {
1436:+                i->setBeeperNetworkBadge(value.toBool());
1437:+                return true;
1438:+            } else
1439:+                return false;
1440:+        }
1441:         case UseIdenticon: {
1442:             if (value.userType() == QMetaType::Bool) {
1443:                 i->setUseIdenticon(value.toBool());
1444:@@ -2283,6 +2642,9 @@ UserSettingsModel::UserSettingsModel(QObject *p)
1445:     connect(s.get(), &UserSettings::avatarCirclesChanged, this, [this]() {
1446:         emit dataChanged(index(AvatarCircles), index(AvatarCircles), {Value});
1447:     });
1448:+    connect(s.get(), &UserSettings::networkOutlineChanged, this, [this]() {
1449:+        emit dataChanged(index(NetworkOutline), index(NetworkOutline), {Value});
1450:+    });
1451:     connect(s.get(), &UserSettings::useIdenticonChanged, this, [this]() {
1452:         emit dataChanged(index(UseIdenticon), index(UseIdenticon), {Value});
1453:     });
1454:diff --git a/src/UserSettingsPage.h b/src/UserSettingsPage.h
1455:index c1c198d..c094d1a 100644
1456:--- a/src/UserSettingsPage.h
1457:+++ b/src/UserSettingsPage.h
1458:@@ -12,6 +12,19 @@
1459: 
1460: #include <optional>
1461: 
1462:+struct CustomLabel
1463:+{
1464:+    Q_GADGET
1465:+    Q_PROPERTY(QString tag MEMBER tag CONSTANT)
1466:+    Q_PROPERTY(QString displayName MEMBER displayName CONSTANT)
1467:+    Q_PROPERTY(QString iconKey MEMBER iconKey CONSTANT)
1468:+
1469:+public:
1470:+    QString tag;
1471:+    QString displayName;
1472:+    QString iconKey;
1473:+};
1474:+
1475: class UserSettings final : public QObject
1476: {
1477:     Q_OBJECT
1478:@@ -41,6 +54,8 @@ class UserSettings final : public QObject
1479:                  roomSortingChangedImportance)
1480:     Q_PROPERTY(bool sortByAlphabet READ sortByAlphabet WRITE setSortByAlphabet NOTIFY
1481:                  roomSortingChangedAlphabetical)
1482:+    Q_PROPERTY(bool sortUnreadFirst READ sortUnreadFirst WRITE setSortUnreadFirst NOTIFY
1483:+                 sortUnreadFirstChanged)
1484:     Q_PROPERTY(bool buttonsInTimeline READ buttonsInTimeline WRITE setButtonsInTimeline NOTIFY
1485:                  buttonInTimelineChanged)
1486:     Q_PROPERTY(bool readReceipts READ readReceipts WRITE setReadReceipts NOTIFY readReceiptsChanged)
1487:@@ -50,6 +65,10 @@ class UserSettings final : public QObject
1488:                  NOTIFY alertOnNotificationChanged)
1489:     Q_PROPERTY(
1490:       bool avatarCircles READ avatarCircles WRITE setAvatarCircles NOTIFY avatarCirclesChanged)
1491:+    Q_PROPERTY(
1492:+      bool networkOutline READ networkOutline WRITE setNetworkOutline NOTIFY networkOutlineChanged)
1493:+    Q_PROPERTY(
1494:+      bool beeperNetworkBadge READ beeperNetworkBadge WRITE setBeeperNetworkBadge NOTIFY beeperNetworkBadgeChanged)
1495:     Q_PROPERTY(
1496:       bool decryptSidebar READ decryptSidebar WRITE setDecryptSidebar NOTIFY decryptSidebarChanged)
1497:     Q_PROPERTY(bool decryptNotifications READ decryptNotifications WRITE setDecryptNotifications
1498:@@ -123,6 +142,9 @@ class UserSettings final : public QObject
1499:                  updateSpaceViasChanged)
1500:     Q_PROPERTY(bool expireEvents READ expireEvents WRITE setExpireEvents NOTIFY expireEventsChanged)
1501: 
1502:+    // Beeper Custom Labels
1503:+    Q_PROPERTY(QVariantList customLabels READ customLabels NOTIFY customLabelsChanged)
1504:+
1505:     UserSettings();
1506: 
1507: public:
1508:@@ -130,13 +152,9 @@ public:
1509:     static void initialize(std::optional<QString> profile);
1510:     static UserSettings *create(QQmlEngine *qmlEngine, QJSEngine *)
1511:     {
1512:-        // The instance has to exist before it is used. We cannot replace it.
1513:         Q_ASSERT(instance());
1514:-
1515:-        // The engine has to have the same thread affinity as the singleton.
1516:         Q_ASSERT(qmlEngine->thread() == instance()->thread());
1517: 
1518:-        // There can only be one engine accessing the singleton.
1519:         static QJSEngine *s_engine = nullptr;
1520:         if (s_engine)
1521:             Q_ASSERT(qmlEngine == s_engine);
1522:@@ -198,6 +216,7 @@ public:
1523:     void setTypingNotifications(bool state);
1524:     void setSortByImportance(bool state);
1525:     void setSortByAlphabet(bool state);
1526:+    void setSortUnreadFirst(bool state);
1527:     void setButtonsInTimeline(bool state);
1528:     void setTimelineMaxWidth(int state);
1529:     void setCommunityListWidth(int state);
1530:@@ -205,6 +224,8 @@ public:
1531:     void setDesktopNotifications(bool state);
1532:     void setAlertOnNotification(bool state);
1533:     void setAvatarCircles(bool state);
1534:+    void setNetworkOutline(bool state);
1535:+    void setBeeperNetworkBadge(bool state);
1536:     void setDecryptSidebar(bool state);
1537:     void setDecryptNotifications(bool state);
1538:     void setSpaceNotifications(bool state);
1539:@@ -247,6 +268,14 @@ public:
1540:     void setUpdateSpaceVias(bool state);
1541:     void setExpireEvents(bool state);
1542: 
1543:+    // Beeper Custom Labels
1544:+    QVariantList customLabels() const { return customLabels_; }
1545:+    void addCustomLabel(const QString &tag, const QString &displayName, const QString &iconKey);
1546:+    void removeCustomLabel(const QString &tag);
1547:+    void updateCustomLabel(const QString &tag, const QString &displayName, const QString &iconKey);
1548:+    QString customLabelDisplayName(const QString &rawTag) const;
1549:+    QString customLabelIconKey(const QString &rawTag) const;
1550:+
1551:     QString theme() const { return !theme_.isEmpty() ? theme_ : defaultTheme_; }
1552:     bool messageHoverHighlight() const { return messageHoverHighlight_; }
1553:     bool enlargeEmojiOnlyMessages() const { return enlargeEmojiOnlyMessages_; }
1554:@@ -255,6 +284,8 @@ public:
1555:     bool groupView() const { return groupView_; }
1556:     bool scrollbarsInRoomlist() const { return scrollbarsInRoomlist_; }
1557:     bool avatarCircles() const { return avatarCircles_; }
1558:+    bool networkOutline() const { return networkOutline_; }
1559:+    bool beeperNetworkBadge() const { return beeperNetworkBadge_; }
1560:     bool decryptSidebar() const { return decryptSidebar_; }
1561:     bool decryptNotifications() const { return decryptNotifications_; }
1562:     bool spaceNotifications() const { return spaceNotifications_; }
1563:@@ -270,6 +301,7 @@ public:
1564:     bool typingNotifications() const { return typingNotifications_; }
1565:     bool sortByImportance() const { return sortByImportance_; }
1566:     bool sortByAlphabet() const { return sortByAlphabet_; }
1567:+    bool sortUnreadFirst() const { return sortUnreadFirst_; }
1568:     bool buttonsInTimeline() const { return buttonsInTimeline_; }
1569:     bool mobileMode() const { return mobileMode_; }
1570:     bool disableSwipe() const { return disableSwipe_; }
1571:@@ -287,7 +319,6 @@ public:
1572:         if (emojiFont_ == QLatin1String("Default")) {
1573:             return tr("Default");
1574:         }
1575:-
1576:         return emojiFont_;
1577:     }
1578:     Presence presence() const { return presence_; }
1579:@@ -330,6 +361,7 @@ signals:
1580:     void scrollbarsInRoomlistChanged(bool state);
1581:     void roomSortingChangedImportance(bool state);
1582:     void roomSortingChangedAlphabetical(bool state);
1583:+    void sortUnreadFirstChanged(bool state);
1584:     void themeChanged(QString state);
1585:     void messageHoverHighlightChanged(bool state);
1586:     void enlargeEmojiOnlyMessagesChanged(bool state);
1587:@@ -346,6 +378,8 @@ signals:
1588:     void desktopNotificationsChanged(bool state);
1589:     void alertOnNotificationChanged(bool state);
1590:     void avatarCirclesChanged(bool state);
1591:+    void networkOutlineChanged(bool state);
1592:+    void beeperNetworkBadgeChanged(bool state);
1593:     void decryptSidebarChanged(bool state);
1594:     void decryptNotificationsChanged(bool state);
1595:     void spaceNotificationsChanged(bool state);
1596:@@ -385,6 +419,7 @@ signals:
1597:     void useIdenticonChanged(bool state);
1598:     void openImageExternalChanged(bool state);
1599:     void openVideoExternalChanged(bool state);
1600:+    void customLabelsChanged();
1601:     void hiddenPinsChanged();
1602:     void hiddenWidgetsChanged();
1603:     void recentReactionsChanged();
1604:@@ -393,7 +428,6 @@ signals:
1605:     void expireEventsChanged(bool state);
1606: 
1607: private:
1608:-    // Default to system theme if QT_QPA_PLATFORMTHEME var is set.
1609:     QString defaultTheme_ = QProcessEnvironment::systemEnvironment()
1610:                                 .value(QStringLiteral("QT_QPA_PLATFORMTHEME"), QLatin1String(""))
1611:                                 .isEmpty()
1612:@@ -414,11 +448,14 @@ private:
1613:     bool typingNotifications_;
1614:     bool sortByImportance_;
1615:     bool sortByAlphabet_;
1616:+    bool sortUnreadFirst_ = false;
1617:     bool buttonsInTimeline_;
1618:     bool readReceipts_;
1619:     bool hasDesktopNotifications_;
1620:     bool hasAlertOnNotification_;
1621:     bool avatarCircles_;
1622:+    bool networkOutline_ = true;
1623:+    bool beeperNetworkBadge_ = true;
1624:     bool decryptSidebar_;
1625:     bool decryptNotifications_;
1626:     bool spaceNotifications_;
1627:@@ -462,6 +499,9 @@ private:
1628:     QStringList hiddenWidgets_;
1629:     QStringList recentReactions_;
1630:     QList<QStringList> collapsedSpaces_;
1631:+    QVariantList customLabels_;
1632:+    void loadCustomLabels();
1633:+    void saveCustomLabels();
1634:     bool useIdenticon_;
1635:     bool openImageExternal_;
1636:     bool openVideoExternal_;
1637:@@ -564,6 +604,13 @@ class UserSettingsModel : public QAbstractListModel
1638:         DeviceId,
1639:         DeviceFingerprint,
1640: 
1641:+        PatchesSection,
1642:+        NetworkOutline,
1643:+        BeeperNetworkBadge,
1644:+        ForceCacheSync,
1645:+        ForceReinit,
1646:+        ManageCustomLabels,
1647:+
1648:         LoginInfoSection,
1649:         UserId,
1650:         Homeserver,
1651:@@ -571,7 +618,6 @@ class UserSettingsModel : public QAbstractListModel
1652:         Version,
1653:         Platform,
1654:         COUNT,
1655:-        // hidden for now
1656:         AccessToken,
1657: #ifdef Q_OS_MACOS
1658:         ScaleFactor,
1659:@@ -596,6 +642,9 @@ public:
1660:         XSignKeysRequestDownload,
1661:         ConfigureHiddenEvents,
1662:         ManageIgnoredUsers,
1663:+        ForceCacheSyncAction,
1664:+        ForceReinitAction,
1665:+        ManageCustomLabelsAction,
1666:     };
1667:     Q_ENUM(Types);
1668: 
1669:@@ -628,3 +677,50 @@ public:
1670:     Q_INVOKABLE void requestCrossSigningSecrets();
1671:     Q_INVOKABLE void downloadCrossSigningSecrets();
1672: };
1673:+
1674:+//! QML-accessible singleton for managing custom Beeper labels.
1675:+class CustomLabelListModel : public QAbstractListModel
1676:+{
1677:+    Q_OBJECT
1678:+    QML_ELEMENT
1679:+    QML_SINGLETON
1680:+
1681:+public:
1682:+    enum Roles
1683:+    {
1684:+        TagRole = Qt::UserRole,
1685:+        DisplayNameRole,
1686:+        IconKeyRole,
1687:+    };
1688:+
1689:+    CustomLabelListModel(QObject *parent = nullptr);
1690:+    static CustomLabelListModel *create(QQmlEngine *, QJSEngine *);
1691:+
1692:+    QHash<int, QByteArray> roleNames() const override;
1693:+    int rowCount(const QModelIndex &parent = QModelIndex()) const override
1694:+    {
1695:+        (void)parent;
1696:+        return labels_.size();
1697:+    }
1698:+    QVariant data(const QModelIndex &index, int role) const override;
1699:+
1700:+    Q_INVOKABLE void addLabel(const QString &tag,
1701:+                              const QString &displayName,
1702:+                              const QString &iconKey);
1703:+    Q_INVOKABLE void removeLabel(int row);
1704:+    Q_INVOKABLE void updateLabel(int row,
1705:+                                 const QString &tag,
1706:+                                 const QString &displayName,
1707:+                                 const QString &iconKey);
1708:+    Q_INVOKABLE QStringList availableIcons() const;
1709:+    Q_INVOKABLE QString displayNameForTag(const QString &tag) const;
1710:+    Q_INVOKABLE QString iconKeyForTag(const QString &tag) const;
1711:+
1712:+private slots:
1713:+    void refreshFromSettings();
1714:+
1715:+private:
1716:+    QList<CustomLabel> labels_;
1717:+
1718:+    inline static CustomLabelListModel *instance_ = nullptr;
1719:+};
1720:diff --git a/src/main.cpp b/src/main.cpp
1721:index bc13cdc..f042090 100644
1722:--- a/src/main.cpp
1723:+++ b/src/main.cpp
1724:@@ -5,6 +5,7 @@
1725: #include <iostream>
1726: 
1727: #include <QApplication>
1728:+#include <QPixmapCache>
1729: #include <QCommandLineParser>
1730: #include <QDesktopServices>
1731: #include <QDir>
1732:@@ -186,6 +187,11 @@ main(int argc, char *argv[])
1733: 
1734:     QApplication app(argc, argv);
1735: 
1736:+    // Increase the QPixmapCache limit to 50 MB so frequently-used avatars
1737:+    // stay in VRAM memory, avoiding re-decodes from disk during scroll.
1738:+    // Default is ~10 MB which is insufficient for 200+ room avatars.
1739:+    QPixmapCache::setCacheLimit(50 * 1024);
1740:+
1741:     QCommandLineParser parser;
1742:     parser.addHelpOption();
1743:     parser.addVersionOption();
1744:diff --git a/src/timeline/CommunitiesModel.cpp b/src/timeline/CommunitiesModel.cpp
1745:index a707c10..24ab982 100644
1746:--- a/src/timeline/CommunitiesModel.cpp
1747:+++ b/src/timeline/CommunitiesModel.cpp
1748:@@ -5,6 +5,7 @@
1749: #include "CommunitiesModel.h"
1750: 
1751: #include <mtx/responses/common.hpp>
1752:+#include <mtx/responses.hpp>
1753: #include <set>
1754:@@ -191,11 +191,19 @@ CommunitiesModel::data(const QModelIndex &index, int role) const
1755:                 return tr("Messages from your server or administrator.");
1756:             }
1757:         } else {
1758:+            // Check for Beeper custom label override
1759:+            auto customName = UserSettings::instance()->customLabelDisplayName(tag);
1760:+            auto customIcon = UserSettings::instance()->customLabelIconKey(tag);
1761:+
1762:             switch (role) {
1763:             case CommunitiesModel::Roles::AvatarUrl:
1764:+                if (!customIcon.isEmpty())
1765:+                    return customIcon;
1766:                 return QStringLiteral(":/icons/icons/ui/tag.svg");
1767:             case CommunitiesModel::Roles::DisplayName:
1768:             case CommunitiesModel::Roles::Tooltip:
1769:+                if (!customName.isEmpty())
1770:+                    return customName;
1771:                 return tag.mid(2);
1772:             }
1773:         }
1774:@@ -937,4 +945,93 @@ CommunitiesModel::updateSpaceStatus(QString space,
1775:     }
1776: }
1777: 
1778:+// -----------------------------------------------------------------------
1779:+// Space CRUD
1780:+// -----------------------------------------------------------------------
1781:+void
1782:+CommunitiesModel::createSpace(const QString &name, const QString &topic, const QString &avatarUrl, bool isPublic)
1783:+{
1784:+    mtx::requests::CreateRoom req;
1785:+    req.name          = name.toStdString();
1786:+    req.topic         = topic.toStdString();
1787:+    req.creation_content       = mtx::events::state::Create{};
1788:+    req.creation_content->type = mtx::events::state::room_type::space;
1789:+    req.preset        = isPublic ? mtx::requests::Preset::PublicChat : mtx::requests::Preset::PrivateChat;
1790:+
1791:+    if (!avatarUrl.isEmpty()) {
1792:+        mtx::events::StrippedEvent<mtx::events::state::Avatar> avatarEvent;
1793:+        avatarEvent.type              = mtx::events::EventType::RoomAvatar;
1794:+        avatarEvent.content.url = avatarUrl.toStdString();
1795:+        req.initial_state.emplace_back(std::move(avatarEvent));
1796:+    }
1797:+
1798:+    http::client()->create_room(
1799:+      req,
1800:+      [this](const mtx::responses::CreateRoom &res, mtx::http::RequestErr err) {
1801:+          if (err) {
1802:+              QString msg = QString::fromStdString(err->matrix_error.error);
1803:+              nhlog::ui()->error("Failed to create space: {}", msg.toStdString());
1804:+              emit spaceError(tr("Failed to create space: %1").arg(msg));
1805:+              return;
1806:+          }
1807:+          QString roomId = QString::fromStdString(res.room_id.to_string());
1808:+          nhlog::ui()->info("Created space: {}", roomId.toStdString());
1809:+          initializeSidebar();
1810:+          emit spaceCreated(roomId);
1811:+      });
1812:+}
1813:+
1814:+void
1815:+CommunitiesModel::updateSpace(const QString &spaceId, const QString &name, const QString &topic, const QString &avatarUrl)
1816:+{
1817:+    auto room_id = spaceId.toStdString();
1818:+
1819:+    // Send m.room.name if provided
1820:+    if (!name.isEmpty()) {
1821:+        mtx::events::state::Name nameEvent;
1822:+        nameEvent.name = name.toStdString();
1823:+        http::client()->send_state_event(
1824:+          room_id, nameEvent, [spaceId](const mtx::responses::EventId &, mtx::http::RequestErr err) {
1825:+              if (err)
1826:+                  nhlog::ui()->error("Failed to update space name: {}",
1827:+                                     err->matrix_error.error);
1828:+          });
1829:+    }
1830:+
1831:+    // Send m.room.topic if provided
1832:+    if (!topic.isEmpty()) {
1833:+        mtx::events::state::Topic topicEvent;
1834:+        topicEvent.topic = topic.toStdString();
1835:+        http::client()->send_state_event(
1836:+          room_id, topicEvent, [spaceId](const mtx::responses::EventId &, mtx::http::RequestErr err) {
1837:+              if (err)
1838:+                  nhlog::ui()->error("Failed to update space topic: {}",
1839:+                                     err->matrix_error.error);
1840:+          });
1841:+    }
1842:+
1843:+    // Send m.room.avatar if provided
1844:+    if (!avatarUrl.isEmpty()) {
1845:+        mtx::events::state::Avatar avatarEvent;
1846:+        avatarEvent.url = avatarUrl.toStdString();
1847:+        http::client()->send_state_event(
1848:+          room_id, avatarEvent, [spaceId](const mtx::responses::EventId &, mtx::http::RequestErr err) {
1849:+              if (err)
1850:+                  nhlog::ui()->error("Failed to update space avatar: {}",
1851:+                                     err->matrix_error.error);
1852:+          });
1853:+    }
1854:+
1855:+    emit spaceUpdated(spaceId);
1856:+}
1857:+
1858:+void
1859:+CommunitiesModel::deleteSpace(const QString &spaceId)
1860:+{
1861:+    // Leave the space room
1862:+    ChatPage::instance()->leaveRoom(spaceId, QStringLiteral("Space deleted"));
1863:+    emit spaceDeleted(spaceId);
1864:+    initializeSidebar();
1865:+}
1866:+
1867: #include "moc_CommunitiesModel.cpp"
1868:diff --git a/src/timeline/CommunitiesModel.h b/src/timeline/CommunitiesModel.h
1869:index 7af6d05..2d941d4 100644
1870:--- a/src/timeline/CommunitiesModel.h
1871:+++ b/src/timeline/CommunitiesModel.h
1872:@@ -198,6 +198,9 @@ public:
1873:                                        bool setParent,
1874:                                        bool setChild,
1875:                                        bool canonical) const;
1876:+    Q_INVOKABLE void createSpace(const QString &name, const QString &topic, const QString &avatarUrl, bool isPublic);
1877:+    Q_INVOKABLE void updateSpace(const QString &spaceId, const QString &name, const QString &topic, const QString &avatarUrl);
1878:+    Q_INVOKABLE void deleteSpace(const QString &spaceId);
1879: 
1880: public slots:
1881:     void initializeSidebar();
1882:@@ -231,6 +234,10 @@ signals:
1883:     void hiddenTagsChanged();
1884:     void tagsChanged();
1885:     void containsSubspacesChanged();
1886:+    void spaceCreated(const QString &spaceId);
1887:+    void spaceUpdated(const QString &spaceId);
1888:+    void spaceDeleted(const QString &spaceId);
1889:+    void spaceError(const QString &message);
1890: 
1891: private:
1892:     QStringList tags_;
1893:diff --git a/src/timeline/RoomlistModel.cpp b/src/timeline/RoomlistModel.cpp
1894:index ce2a8f8..5e9d49f 100644
1895:--- a/src/timeline/RoomlistModel.cpp
1896:+++ b/src/timeline/RoomlistModel.cpp
1897:@@ -14,6 +14,7 @@
1898: #include "MainWindow.h"
1899: #include "MatrixClient.h"
1900: #include "MxcImageProvider.h"
1901:+#include "AvatarProvider.h"
1902: #include "TimelineModel.h"
1903: #include "TimelineViewManager.h"
1904: #include "UserSettingsPage.h"
1905:@@ -24,6 +25,90 @@
1906: #include <QDBusConnection>
1907: #endif
1908: 
1909:+namespace {
1910:+// ---------------------------------------------------------------------------
1911:+// Beeper MXID-based network detection helpers
1912:+// ---------------------------------------------------------------------------
1913:+
1914:+// Bridge MXID localpart prefix → {display name, brand hex color}
1915:+static const QHash<QString, QPair<QString, QColor>> kBeeperPrefixMap = {
1916:+    {"whatsapp_",     {QStringLiteral("WhatsApp"),     QColor(0x25, 0xD3, 0x66)}},
1917:+    {"whatsappgo_",   {QStringLiteral("WhatsApp"),     QColor(0x25, 0xD3, 0x66)}},
1918:+    {"instagram_",    {QStringLiteral("Instagram"),    QColor(0xE1, 0x30, 0x6C)}},
1919:+    {"instagramgo_",  {QStringLiteral("Instagram"),    QColor(0xE1, 0x30, 0x6C)}},
1920:+    {"telegram_",     {QStringLiteral("Telegram"),     QColor(0x2A, 0xAB, 0xEE)}},
1921:+    {"telegramgo_",   {QStringLiteral("Telegram"),     QColor(0x2A, 0xAB, 0xEE)}},
1922:+    {"signal_",       {QStringLiteral("Signal"),       QColor(0x3A, 0x76, 0xF0)}},
1923:+    {"signalgo_",     {QStringLiteral("Signal"),       QColor(0x3A, 0x76, 0xF0)}},
1924:+    {"discord_",      {QStringLiteral("Discord"),      QColor(0x58, 0x65, 0xF2)}},
1925:+    {"discordgo_",    {QStringLiteral("Discord"),      QColor(0x58, 0x65, 0xF2)}},
1926:+    {"facebook_",     {QStringLiteral("Messenger"),    QColor(0x00, 0xB2, 0xFF)}},
1927:+    {"facebookgo_",   {QStringLiteral("Messenger"),    QColor(0x00, 0xB2, 0xFF)}},
1928:+    {"slack_",        {QStringLiteral("Slack"),        QColor(0x4A, 0x15, 0x4B)}},
1929:+    {"slackgo_",      {QStringLiteral("Slack"),        QColor(0x4A, 0x15, 0x4B)}},
1930:+    {"googlechat_",   {QStringLiteral("Google Chat"),  QColor(0x34, 0xA8, 0x53)}},
1931:+    {"googlechatgo_", {QStringLiteral("Google Chat"),  QColor(0x34, 0xA8, 0x53)}},
1932:+    {"linkedin_",     {QStringLiteral("LinkedIn"),     QColor(0x0A, 0x66, 0xC2)}},
1933:+    {"linkedingo_",   {QStringLiteral("LinkedIn"),     QColor(0x0A, 0x66, 0xC2)}},
1934:+    {"twitter_",      {QStringLiteral("Twitter/X"),    QColor(0x00, 0x00, 0x00)}},
1935:+    {"twittergo_",    {QStringLiteral("Twitter/X"),    QColor(0x00, 0x00, 0x00)}},
1936:+    {"sms_",          {QStringLiteral("SMS"),          QColor(0x34, 0xB7, 0xF1)}},
1937:+    {"smsgo_",        {QStringLiteral("SMS"),          QColor(0x34, 0xB7, 0xF1)}},
1938:+    {"imessage_",     {QStringLiteral("iMessage"),     QColor(0x34, 0xC7, 0x59)}},
1939:+    {"imessagego_",   {QStringLiteral("iMessage"),     QColor(0x34, 0xC7, 0x59)}},
1940:+    {"groupme_",      {QStringLiteral("GroupMe"),      QColor(0x00, 0xAF, 0xF0)}},
1941:+    {"groupmego_",    {QStringLiteral("GroupMe"),      QColor(0x00, 0xAF, 0xF0)}},
1942:+    {"skype_",        {QStringLiteral("Skype"),        QColor(0x00, 0xAF, 0xF0)}},
1943:+    {"skypego_",      {QStringLiteral("Skype"),        QColor(0x00, 0xAF, 0xF0)}},
1944:+    {"wechat_",       {QStringLiteral("WeChat"),       QColor(0x07, 0xC1, 0x60)}},
1945:+    {"wechatgo_",     {QStringLiteral("WeChat"),       QColor(0x07, 0xC1, 0x60)}},
1946:+    {"line_",         {QStringLiteral("LINE"),         QColor(0x00, 0xC3, 0x00)}},
1947:+    {"linego_",       {QStringLiteral("LINE"),         QColor(0x00, 0xC3, 0x00)}},
1948:+    {"kakaotalk_",    {QStringLiteral("KakaoTalk"),    QColor(0xFE, 0xE5, 0x00)}},
1949:+    {"kakaotalkgo_",  {QStringLiteral("KakaoTalk"),    QColor(0xFE, 0xE5, 0x00)}},
1950:+};
1951:+
1952:+// Given a fully-qualified MXID, extract the localpart prefix and look up
1953:+// the corresponding bridge network info. Returns empty BeeperNetworkInfo
1954:+// when no known bridge prefix matches.
1955:+BeeperNetworkInfo
1956:+beeperNetworkFromMxid(const QString &mxid)
1957:+{
1958:+    // Parse localpart: @prefix_rest:server
1959:+    const auto atPos = mxid.indexOf(QLatin1Char('@'));
1960:+    if (atPos < 0)
1961:+        return {};
1962:+    const auto colonPos = mxid.indexOf(QLatin1Char(':'), atPos + 1);
1963:+    if (colonPos < 0)
1964:+        return {};
1965:+
1966:+    const auto localpart = mxid.mid(atPos + 1, colonPos - atPos - 1);
1967:+
1968:+    // Scan prefix map (longer prefixes first for correctness with e.g.
1969:+    // "instagramgo_" vs "instagram_").
1970:+    for (auto it = kBeeperPrefixMap.constBegin(); it != kBeeperPrefixMap.constEnd(); ++it) {
1971:+        if (localpart.startsWith(it.key(), Qt::CaseInsensitive))
1972:+            return {it.value().first, it.value().second, QString()};
1973:+    }
1974:+
1975:+    // Fallback: extract text before the first underscore, strip trailing
1976:+    // "go" suffix, capitalize.
1977:+    const auto underscorePos = localpart.indexOf(QLatin1Char('_'));
1978:+    if (underscorePos > 0) {
1979:+        auto prefix = localpart.left(underscorePos);
1980:+        if (prefix.endsWith(QStringLiteral("go"), Qt::CaseInsensitive))
1981:+            prefix.chop(2);
1982:+        if (!prefix.isEmpty()) {
1983:+            prefix[0] = prefix[0].toUpper();
1984:+            return {prefix, QColor(), QString()};
1985:+        }
1986:+    }
1987:+
1988:+    return {QString(), QColor(), QString()};
1989:+}
1990:+
1991:+} // namespace
1992:+
1993: RoomlistModel::RoomlistModel(TimelineViewManager *parent)
1994:   : QAbstractListModel(parent)
1995:   , manager(parent)
1996:@@ -87,6 +172,9 @@ RoomlistModel::roleNames() const
1997:       {ParentSpaces, "parentSpaces"},
1998:       {IsDirect, "isDirect"},
1999:       {DirectChatOtherUserId, "directChatOtherUserId"},
2000:+      {BeeperNetworkRole, "beeperNetwork"},
2001:+      {BeeperNetworkColorRole, "beeperNetworkColor"},
2002:+      {BeeperNetworkOtherUserId, "beeperNetworkOtherUserId"},
2003:     };
2004: }
2005: 
2006:@@ -145,6 +233,12 @@ RoomlistModel::data(const QModelIndex &index, int role) const
2007:                     list.push_back(QString::fromStdString(t));
2008:                 return list;
2009:             }
2010:+            case Roles::BeeperNetworkRole:
2011:+                return networkCache.value(roomid).name;
2012:+            case Roles::BeeperNetworkColorRole:
2013:+                return networkCache.value(roomid).color;
2014:+            case Roles::BeeperNetworkOtherUserId:
2015:+                return networkCache.value(roomid).counterpartMxid;
2016:             default:
2017:                 return {};
2018:             }
2019:@@ -174,6 +268,12 @@ RoomlistModel::data(const QModelIndex &index, int role) const
2020:                 return false;
2021:             case Roles::Tags:
2022:                 return QStringList();
2023:+            case Roles::BeeperNetworkRole:
2024:+                return QString();
2025:+            case Roles::BeeperNetworkColorRole:
2026:+                return QString();
2027:+            case Roles::BeeperNetworkOtherUserId:
2028:+                return QString();
2029:             default:
2030:                 return {};
2031:             }
2032:@@ -205,6 +305,12 @@ RoomlistModel::data(const QModelIndex &index, int role) const
2033:                 return true;
2034:             case Roles::Tags:
2035:                 return QStringList();
2036:+            case Roles::BeeperNetworkRole:
2037:+                return QString();
2038:+            case Roles::BeeperNetworkColorRole:
2039:+                return QString();
2040:+            case Roles::BeeperNetworkOtherUserId:
2041:+                return QString();
2042:             default:
2043:                 return {};
2044:             }
2045:@@ -236,6 +342,12 @@ RoomlistModel::data(const QModelIndex &index, int role) const
2046:                 return false;
2047:             case Roles::Tags:
2048:                 return QStringList();
2049:+            case Roles::BeeperNetworkRole:
2050:+                return QString();
2051:+            case Roles::BeeperNetworkColorRole:
2052:+                return QString();
2053:+            case Roles::BeeperNetworkOtherUserId:
2054:+                return QString();
2055:             default:
2056:                 return {};
2057:             }
2058:@@ -374,6 +486,7 @@ RoomlistModel::addRoom(const QString &room_id, bool suppressInsertNotification)
2059:               (int)(roomids.size() + previewsToAdd.size() - ((wasInvite || wasPreview) ? 1 : 0)));
2060: 
2061:         models.insert(room_id, std::move(newRoom));
2062:+        updateNetworkCache(room_id);
2063:         if (wasInvite) {
2064:             auto idx = roomidToIndex(room_id);
2065:             invites.remove(room_id);
2066:@@ -518,8 +631,12 @@ RoomlistModel::sync(const mtx::responses::Sync &sync_)
2067:               std::get_if<mtx::events::AccountDataEvent<mtx::events::account_data::Direct>>(&e)) {
2068:             auto updatedDMs = updateDMs(*event);
2069:             for (const auto &r : updatedDMs) {
2070:-                if (auto idx = roomidToIndex(r); idx != -1)
2071:-                    emit dataChanged(index(idx), index(idx), {IsDirect, DirectChatOtherUserId});
2072:+                if (auto idx = roomidToIndex(r); idx != -1) {
2073:+                    updateNetworkCache(r);
2074:+                    emit dataChanged(index(idx),
2075:+                                     index(idx),
2076:+                                     {IsDirect, DirectChatOtherUserId, BeeperNetworkRole, BeeperNetworkColorRole});
2077:+                }
2078:             }
2079:         }
2080:     }
2081:@@ -559,8 +676,10 @@ RoomlistModel::sync(const mtx::responses::Sync &sync_)
2082:         for (const auto &e : room.account_data.events) {
2083:             if (std::holds_alternative<
2084:                   mtx::events::AccountDataEvent<mtx::events::account_data::Tags>>(e)) {
2085:-                if (auto idx = roomidToIndex(qroomid); idx != -1)
2086:-                    emit dataChanged(index(idx), index(idx), {Tags});
2087:+                if (auto idx = roomidToIndex(qroomid); idx != -1) {
2088:+                    updateNetworkCache(qroomid);
2089:+                    emit dataChanged(index(idx), index(idx), {Tags, BeeperNetworkRole, BeeperNetworkColorRole});
2090:+                }
2091:             }
2092:         }
2093:     }
2094:@@ -636,6 +755,11 @@ RoomlistModel::initializeRooms()
2095: 
2096:     endResetModel();
2097: 
2098:+    // Rebuild network cache to ensure all rooms have entries
2099:+    // (addRoom already calls updateNetworkCache for each room,
2100:+    // but this catches any edge cases).
2101:+    rebuildNetworkCache();
2102:+
2103: #ifdef NHEKO_DBUS_SYS
2104:     if (MainWindow::instance()->dbusAvailable()) {
2105:         dbusInterface_ = new NhekoDBusBackend{this};
2106:@@ -653,6 +777,7 @@ RoomlistModel::clear()
2107:     models.clear();
2108:     invites.clear();
2109:     roomids.clear();
2110:+    networkCache.clear();
2111:     currentRoom_ = nullptr;
2112:     emit currentRoomChanged("");
2113:     endResetModel();
2114:@@ -828,13 +953,60 @@ RoomlistModel::refetchOnlineKeyBackupKeys()
2115:     }
2116: }
2117: 
2118:+void
2119:+RoomlistModel::updateNetworkCache(const QString &roomid)
2120:+{
2121:+    // 1. Check standard DMs
2122:+    if (directChatToUser.count(roomid)) {
2123:+        const auto &userId = directChatToUser.at(roomid).front();
2124:+        auto info          = beeperNetworkFromMxid(userId);
2125:+        networkCache.insert(roomid, {info.name, info.color, userId});
2126:+        return;
2127:+    }
2128:+
2129:+    // 2. Check Beeper fake DMs (3-member rooms with bridge bot)
2130:+    if (models.contains(roomid)) {
2131:+        auto room  = models.value(roomid);
2132:+        auto local = cache::client()->localUserId();
2133:+        // Only check if it looks like a 1:1 or 3-member group via member count.
2134:+        // We query at most 4 members for efficiency.
2135:+        auto members = cache::client()->getMembers(roomid.toStdString(), 0, 4);
2136:+        if (members.size() == 3) {
2137:+            for (const auto &m : members) {
2138:+                if (m.user_id == local)
2139:+                    continue;
2140:+                if (m.user_id.contains(QStringLiteral("bot"), Qt::CaseInsensitive) &&
2141:+                    m.user_id.contains(QStringLiteral(":beeper.")))
2142:+                    continue;
2143:+                // This is the real contact
2144:+                auto info = beeperNetworkFromMxid(m.user_id);
2145:+                networkCache.insert(roomid, {info.name, info.color, m.user_id});
2146:+                return;
2147:+            }
2148:+        }
2149:+    }
2150:+
2151:+    // No bridge detected
2152:+    networkCache.insert(roomid, {QString(), QColor(), QString()});
2153:+}
2154:+
2155:+void
2156:+RoomlistModel::rebuildNetworkCache()
2157:+{
2158:+    networkCache.clear();
2159:+    networkCache.reserve(static_cast<int>(roomids.size()));
2160:+    for (const auto &roomid : roomids)
2161:+        updateNetworkCache(roomid);
2162:+}
2163:+
2164: namespace {
2165: enum NotificationImportance : short
2166: {
2167:-    NoPreview          = -3,
2168:-    Preview            = -2,
2169:-    ImportanceDisabled = -1,
2170:-    AllEventsRead      = 0,
2171:+    NoPreview          = -4,
2172:+    Preview            = -3,
2173:+    ImportanceDisabled = -2,
2174:+    AllEventsRead      = -1,
2175:+    UnreadFirst        = 0,
2176:     NewMessage         = 1,
2177:     NewMentions        = 2,
2178:     Invite             = 3,
2179:@@ -862,12 +1034,15 @@ FilteredRoomlistModel::calculateImportance(const QModelIndex &idx) const
2180:             return NoPreview;
2181:     } else if (sourceModel()->data(idx, RoomlistModel::IsInvite).toBool()) {
2182:         return Invite;
2183:-    } else if (!this->sortByImportance) {
2184:+    } else if (!this->sortByImportance && !this->sortUnreadFirst) {
2185:         return ImportanceDisabled;
2186:     } else if (sourceModel()->data(idx, RoomlistModel::HasLoudNotification).toBool()) {
2187:         return NewMentions;
2188:     } else if (sourceModel()->data(idx, RoomlistModel::NotificationCount).toInt() > 0) {
2189:         return NewMessage;
2190:+    } else if (this->sortUnreadFirst &&
2191:+               sourceModel()->data(idx, RoomlistModel::HasUnreadMessages).toBool()) {
2192:+        return UnreadFirst;
2193:     } else {
2194:         return AllEventsRead;
2195:     }
2196:@@ -919,6 +1094,7 @@ FilteredRoomlistModel::FilteredRoomlistModel(RoomlistModel *model, QObject *pare
2197: 
2198:     this->sortByImportance = UserSettings::instance()->sortByImportance();
2199:     this->sortByAlphabet   = UserSettings::instance()->sortByAlphabet();
2200:+    this->sortUnreadFirst  = UserSettings::instance()->sortUnreadFirst();
2201:     setSourceModel(model);
2202:     setDynamicSortFilter(true);
2203: 
2204:@@ -938,6 +1114,22 @@ FilteredRoomlistModel::FilteredRoomlistModel(RoomlistModel *model, QObject *pare
2205:                          invalidate();
2206:                      });
2207: 
2208:+    QObject::connect(UserSettings::instance().get(),
2209:+                     &UserSettings::sortUnreadFirstChanged,
2210:+                     this,
2211:+                     [this](bool sortUnreadFirst_) {
2212:+                         this->sortUnreadFirst = sortUnreadFirst_;
2213:+                         invalidate();
2214:+                     });
2215:+
2216:     connect(roomlistmodel,
2217:             &RoomlistModel::currentRoomChanged,
2218:             this,
2219:@@ -1200,6 +1392,51 @@ FilteredRoomlistModel::toggleTag(const QString &roomid, const QString &tag, bool
2220:     }
2221: }
2222: 
2223:+QColor
2224:+FilteredRoomlistModel::networkColorForMxid(const QString &mxid) const
2225:+{
2226:+    // Reuse the same beeper prefix lookup logic from the anonymous namespace
2227:+    const auto atPos = mxid.indexOf(QLatin1Char('@'));
2228:+    if (atPos < 0)
2229:+        return {};
2230:+    const auto colonPos = mxid.indexOf(QLatin1Char(':'), atPos + 1);
2231:+    if (colonPos < 0)
2232:+        return {};
2233:+
2234:+    const auto localpart = mxid.mid(atPos + 1, colonPos - atPos - 1);
2235:+
2236:+    // kBeeperPrefixMap logic duplicated inline for access from the method
2237:+    static const QHash<QString, QPair<QString, QColor>> prefixMap = {
2238:+        {"whatsapp_",     {QStringLiteral("WhatsApp"),     QColor(0x25, 0xD3, 0x366)}},
2239:+        {"whatsappgo_",   {QStringLiteral("WhatsApp"),     QColor(0x25, 0xD3, 0x366)}},
2240:+        {"instagram_",    {QStringLiteral("Instagram"),    QColor(0xE1, 0x30, 0x6C)}},
2241:+        {"instagramgo_",  {QStringLiteral("Instagram"),    QColor(0xE1, 0x30, 0x6C)}},
2242:+        {"telegram_",     {QStringLiteral("Telegram"),     QColor(0x2A, 0xAB, 0xEE)}},
2243:+        {"telegramgo_",   {QStringLiteral("Telegram"),     QColor(0x2A, 0xAB, 0xEE)}},
2244:+        {"signal_",       {QStringLiteral("Signal"),       QColor(0x3A, 0x76, 0xF0)}},
2245:+        {"signalgo_",     {QStringLiteral("Signal"),       QColor(0x3A, 0x76, 0xF0)}},
2246:+        {"discord_",      {QStringLiteral("Discord"),      QColor(0x58, 0x65, 0xF2)}},
2247:+        {"discordgo_",    {QStringLiteral("Discord"),      QColor(0x58, 0x65, 0xF2)}},
2248:+        {"facebook_",     {QStringLiteral("Messenger"),    QColor(0x00, 0xB2, 0xFF)}},
2249:+        {"facebookgo_",   {QStringLiteral("Messenger"),    QColor(0x00, 0xB2, 0xFF)}},
2250:+        {"slack_",        {QStringLiteral("Slack"),        QColor(0x4A, 0x15, 0x4B)}},
2251:+        {"slackgo_",      {QStringLiteral("Slack"),        QColor(0x4A, 0x15, 0x4B)}},
2252:+        {"googlechat_",   {QStringLiteral("Google Chat"),  QColor(0x34, 0xA8, 0x53)}},
2253:+        {"googlechatgo_", {QStringLiteral("Google Chat"),  QColor(0x34, 0xA8, 0x53)}},
2254:+        {"sms_",          {QStringLiteral("SMS"),          QColor(0x34, 0xB7, 0xF1)}},
2255:+        {"smsgo_",        {QStringLiteral("SMS"),          QColor(0x34, 0xB7, 0xF1)}},
2256:+        {"imessage_",     {QStringLiteral("iMessage"),     QColor(0x34, 0xC7, 0x59)}},
2257:+        {"imessagego_",   {QStringLiteral("iMessage"),     QColor(0x34, 0xC7, 0x59)}},
2258:+    };
2259:+
2260:+    for (auto it = prefixMap.constBegin(); it != prefixMap.constEnd(); ++it) {
2261:+        if (localpart.startsWith(it.key(), Qt::CaseInsensitive))
2262:+            return it.value().second;
2263:+    }
2264:+
2265:+    return {};
2266:+}
2267:+
2268: void
2269: FilteredRoomlistModel::copyLink(QString roomid)
2270: {
2271:diff --git a/src/timeline/RoomlistModel.h b/src/timeline/RoomlistModel.h
2272:index 7dfeb70..cb63138 100644
2273:--- a/src/timeline/RoomlistModel.h
2274:+++ b/src/timeline/RoomlistModel.h
2275:@@ -6,6 +6,7 @@
2276: 
2277: #include <CacheStructs.h>
2278: #include <QAbstractListModel>
2279:+#include <QColor>
2280: #include <QHash>
2281: #include <QQmlEngine>
2282: #include <QSharedPointer>
2283:@@ -19,6 +20,13 @@
2284: #include "dbus/NhekoDBusBackend.h"
2285: #endif
2286: 
2287:+struct BeeperNetworkInfo
2288:+{
2289:+    QString name;            // e.g. "WhatsApp"
2290:+    QColor color;            // e.g. #25D366
2291:+    QString counterpartMxid; // The real contact's MXID (for avatar outlining)
2292:+};
2293:+
2294: class TimelineModel;
2295: class TimelineViewManager;
2296: 
2297:@@ -81,6 +89,9 @@ public:
2298:         ParentSpaces,
2299:         IsDirect,
2300:         DirectChatOtherUserId,
2301:+        BeeperNetworkRole,
2302:+        BeeperNetworkColorRole,
2303:+        BeeperNetworkOtherUserId,
2304:     };
2305: 
2306:     RoomlistModel(TimelineViewManager *parent = nullptr);
2307:@@ -129,6 +140,12 @@ public slots:
2308:         emit currentRoomChanged("");
2309:     }
2310: 
2311:+    //! Update network cache for a single room by computing MXID-based info.
2312:+    void updateNetworkCache(const QString &roomid);
2313:+
2314:+    //! Recompute network cache for all rooms (called on account data change).
2315:+    void rebuildNetworkCache();
2316:+
2317: private slots:
2318:     void updateReadStatus(const std::map<QString, bool> &roomReadStatus_);
2319: 
2320:@@ -155,6 +172,9 @@ private:
2321: 
2322:     std::map<QString, std::vector<QString>> directChatToUser;
2323: 
2324:+    //! Cache of MXID-based Beeper network info for O(1) data() access.
2325:+    QHash<QString, BeeperNetworkInfo> networkCache;
2326:+
2327: #ifdef NHEKO_DBUS_SYS
2328:     NhekoDBusBackend *dbusInterface_;
2329:     friend class NhekoDBusBackend;
2330:@@ -195,6 +215,7 @@ public slots:
2331:     void leave(QString roomid, QString reason = "") { roomlistmodel->leave(roomid, reason); }
2332:     void toggleTag(const QString &roomid, const QString &tag, bool on);
2333:     void copyLink(QString roomid);
2334:+    Q_INVOKABLE QColor networkColorForMxid(const QString &mxid) const;
2335: 
2336:     TimelineModel *currentRoom() const { return roomlistmodel->currentRoom(); }
2337:     RoomPreview currentRoomPreview() const { return roomlistmodel->currentRoomPreview(); }
2338:@@ -248,6 +269,7 @@ private:
2339:     RoomlistModel *roomlistmodel;
2340:     bool sortByImportance = true;
2341:     bool sortByAlphabet   = false;
2342:+    bool sortUnreadFirst  = false;
2343: 
2344:     enum class FilterBy
2345:     {
2346:diff --git a/src/timeline/TimelineModel.cpp b/src/timeline/TimelineModel.cpp
2347:index 6233682..f046327 100644
2348:--- a/src/timeline/TimelineModel.cpp
2349:+++ b/src/timeline/TimelineModel.cpp
2350:@@ -3424,7 +3424,143 @@ TimelineModel::showImage() const
2351:     case UserSettings::ShowImage::Never:
2352:     default:
2353:         return false;
2354:     }
2355: }
2356: 
2357: //
2358: // Chat History Export
2359: //
2360:+    void
2361:+    TimelineModel::exportChatHistory(const QString &filePath, const QString &format)
2362:+    {
2363:+        QFile file(filePath);
2364:+        if (!file.open(QIODevice::WriteOnly | QIODevice::Text)) {
2365:+            nhlog::ui()->error("Failed to open file for history export: {}",
2366:+                               filePath.toStdString());
2367:+            return;
2368:+        }
2369:+    
2370:+        QTextStream out(&file);
2371:+        out.setEncoding(QStringConverter::Utf8);
2372:+    
2373:+        if (format == QLatin1String("html"))
2374:+            exportChatHistoryToHtml(out);
2375:+        else if (format == QLatin1String("json"))
2376:+            exportChatHistoryToJson(out);
2377:+        else
2378:+            exportChatHistoryToTxt(out);
2379:+    
2380:+        file.close();
2381:+        nhlog::ui()->info("Exported chat history: {} lines, file: {}",
2382:+                          out.status() == QTextStream::Ok ? "OK" : "ERR",
2383:+                          filePath.toStdString());
2384:+    }
2385:+    
2386:+    void
2387:+    TimelineModel::exportChatHistoryToTxt(QTextStream &out)
2388:+    {
2389:+        const QString sep = QString(60, QLatin1Char('='));
2390:+        out << "Chat History - " << plainRoomName() << " (" << roomId() << ")\n";
2391:+        out << sep << "\n\n";
2392:+    
2393:+        int total = 0;
2394:+        int totalEvents = events.size();
2395:+    
2396:+        // Iterate from oldest (0) to newest (size()-1)
2397:+        for (int i = 0; i < totalEvents; ++i) {
2398:+            const auto evt = events.get(i);
2399:+            if (!evt)
2400:+                continue;
2401:+    
2402:+            const auto sender  = QString::fromStdString(mtx::accessors::sender(*evt));
2403:+            const auto body    = QString::fromStdString(mtx::accessors::body(*evt));
2404:+            const auto msgtype = QString::fromStdString(mtx::accessors::msg_type_str(*evt));
2405:+    
2406:+            out << sender << ": ";
2407:+            if (msgtype == QLatin1String("m.text") || msgtype == QLatin1String("m.notice"))
2408:+                out << body;
2409:+            else if (msgtype == QLatin1String("m.emote"))
2410:+                out << "* " << sender << " " << body;
2411:+            else if (msgtype == QLatin1String("m.image"))
2412:+                out << "[Image] " << body;
2413:+            else if (msgtype == QLatin1String("m.file"))
2414:+                out << "[File] " << body;
2415:+            else
2416:+                out << "[" << msgtype << "] " << body;
2417:+    
2418:+            out << "\n";
2419:+            ++total;
2420:+    
2421:+            if (total % 500 == 0)
2422:+                nhlog::ui()->info("History export progress: {} / {}", total, totalEvents);
2423:+        }
2424:+    
2425:+        out << "\n" << sep << "\n";
2426:+        out << "Total messages: " << total << "\n";
2427:+    }
2428:+    
2429:+    void
2430:+    TimelineModel::exportChatHistoryToHtml(QTextStream &out)
2431:+    {
2432:+        out << "<!DOCTYPE html><html><head><meta charset=\"UTF-8\">\n"
2433:+            << "<title>Chat History - " << plainRoomName().toHtmlEscaped() << "</title>\n"
2434:+            << "<style>body{font-family:sans-serif;max-width:800px;margin:auto;padding:20px}"
2435:+            << ".msg{margin:8px 0}.time{color:#888;font-size:0.85em}.sender{font-weight:bold}"
2436:+            << "</style></head><body>\n"
2437:+            << "<h1>" << plainRoomName().toHtmlEscaped() << "</h1>\n"
2438:+            << "<p>Room: " << roomId().toHtmlEscaped() << "</p>\n"
2439:+            << "<hr>\n";
2440:+    
2441:+        int totalEvents = events.size();
2442:+        for (int i = 0; i < totalEvents; ++i) {
2443:+            const auto evt = events.get(i);
2444:+            if (!evt)
2445:+                continue;
2446:+    
2447:+            const auto sender  = QString::fromStdString(mtx::accessors::sender(*evt)).toHtmlEscaped();
2448:+            const auto body =
2449:+              QString::fromStdString(mtx::accessors::body(*evt)).toHtmlEscaped().replace(
2450:+                QLatin1Char('\n'), QStringLiteral("<br>"));
2451:+    
2452:+            out << "<div class=\"msg\">"
2453:+                << "<span class=\"sender\">" << sender << "</span>: "
2454:+                << "<span class=\"body\">" << body << "</span>"
2455:+                << "</div>\n";
2456:+        }
2457:+    
2458:+        out << "<hr></body></html>\n";
2459:+    }
2460:+    
2461:+    void
2462:+    TimelineModel::exportChatHistoryToJson(QTextStream &out)
2463:+    {
2464:+        out << "{\n";
2465:+        out << "  \"roomName\": " << plainRoomName() << ",\n";
2466:+        out << "  \"roomId\": " << roomId() << ",\n";
2467:+        out << "  \"messages\": [\n";
2468:+    
2469:+        int totalEvents = events.size();
2470:+        bool first = true;
2471:+        for (int i = 0; i < totalEvents; ++i) {
2472:+            const auto evt = events.get(i);
2473:+            if (!evt)
2474:+                continue;
2475:+    
2476:+            if (!first)
2477:+                out << ",\n";
2478:+            first = false;
2479:+    
2480:+            const auto sender  = QString::fromStdString(mtx::accessors::sender(*evt));
2481:+            const auto body    = QString::fromStdString(mtx::accessors::body(*evt));
2482:+            const auto msgtype = QString::fromStdString(mtx::accessors::msg_type_str(*evt));
2483:+    
2484:+            out << "    {\n";
2485:+            out << "      \"sender\": " << sender << ",\n";
2486:+            out << "      \"body\": " << body << ",\n";
2487:+            out << "      \"type\": " << msgtype << "\n";
2488:+            out << "    }";
2489:+        }
2490:+    
2491:+        out << "\n  ]\n}\n";
2492:+    }
2493:+    
2494:+    #include "moc_TimelineModel.cpp"
2495:diff --git a/src/timeline/TimelineModel.h b/src/timeline/TimelineModel.h
2496:index db7cee5..f3abbf7 100644
2497:--- a/src/timeline/TimelineModel.h
2498:+++ b/src/timeline/TimelineModel.h
2499:@@ -356,6 +356,10 @@ public:
2500:     }
2501: 
2502:     Q_INVOKABLE void requestKeyForEvent(const QString &id);
2503:+    Q_INVOKABLE void exportChatHistory(const QString &filePath, const QString &format = "txt");
2504:+    void exportChatHistoryToTxt(QTextStream &out);
2505:+    void exportChatHistoryToHtml(QTextStream &out);
2506:+    void exportChatHistoryToJson(QTextStream &out);
2507: 
2508:     std::vector<::Reaction> reactions(const std::string &event_id)
2509:     {
2510:diff --git a/src/timeline/TimelineViewManager.cpp b/src/timeline/TimelineViewManager.cpp
2511:index 389f34f..627d4f6 100644
2512:--- a/src/timeline/TimelineViewManager.cpp
2513:+++ b/src/timeline/TimelineViewManager.cpp
2514:@@ -229,6 +229,29 @@ TimelineViewManager::openRoomSettings(QString room_id)
2515:     emit openRoomSettingsDialog(settings);
2516: }
2517: 
2518:+void
2519:+TimelineViewManager::downloadHistoryForRoom(QString roomid)
2520:+{
2521:+    auto room = rooms_->getRoomById(roomid);
2522:+    if (!room)
2523:+        return;
2524:+
2525:+    QString filter = tr("Text Files (*.txt);;HTML Files (*.html);;JSON Files (*.json)");
2526:+    QString filePath = QFileDialog::getSaveFileName(
2527:+      nullptr, tr("Save Chat History"), roomid + "_history.txt", filter);
2528:+
2529:+    if (filePath.isEmpty())
2530:+        return;
2531:+
2532:+    QString format = QStringLiteral("txt");
2533:+    if (filePath.endsWith(QStringLiteral(".html"), Qt::CaseInsensitive))
2534:+        format = QStringLiteral("html");
2535:+    else if (filePath.endsWith(QStringLiteral(".json"), Qt::CaseInsensitive))
2536:+        format = QStringLiteral("json");
2537:+
2538:+    room->exportChatHistory(filePath, format);
2539:+}
2540:+
2541: void
2542: TimelineViewManager::openInviteUsers(QString roomId)
2543: {
2544:diff --git a/src/timeline/TimelineViewManager.h b/src/timeline/TimelineViewManager.h
2545:index b116402..875d43d 100644
2546:--- a/src/timeline/TimelineViewManager.h
2547:+++ b/src/timeline/TimelineViewManager.h
2548:@@ -121,6 +121,7 @@ signals:
2549:     void ignoredUsersChanged(const QVector<QString> &ignoredUsers);
2550: 
2551: public slots:
2552:+    void downloadHistoryForRoom(QString roomid);
2553:     void updateReadReceipts(const QString &room_id, const std::vector<QString> &event_ids);
2554:     void receivedSessionKey(const std::string &room_id, const std::string &session_id);
2555:     void initializeRoomlist();
2556:diff --git a/src/ui/UserProfile.cpp b/src/ui/UserProfile.cpp
2557:index d05b6f6..84d7158 100644
2558:--- a/src/ui/UserProfile.cpp
2559:+++ b/src/ui/UserProfile.cpp
2560:@@ -629,4 +629,29 @@ UserProfile::openGlobalProfile()
2561:     emit manager->openGlobalUserProfile(userid_);
2562: }
2563: 
2564:+void
2565:+UserProfile::downloadChatHistory()
2566:+{
2567:+    if (!model) {
2568:+        emit displayError(tr("No room model available."));
2569:+        return;
2570:+    }
2571:+
2572:+    QString filter = tr("Text Files (*.txt);;HTML Files (*.html);;JSON Files (*.json)");
2573:+    QString filePath = QFileDialog::getSaveFileName(
2574:+      nullptr, tr("Save Chat History"), userid_ + "_history.txt", filter);
2575:+
2576:+    if (filePath.isEmpty())
2577:+        return;
2578:+
2579:+    // Determine format from extension
2580:+    QString format = QStringLiteral("txt");
2581:+    if (filePath.endsWith(QStringLiteral(".html"), Qt::CaseInsensitive))
2582:+        format = QStringLiteral("html");
2583:+    else if (filePath.endsWith(QStringLiteral(".json"), Qt::CaseInsensitive))
2584:+        format = QStringLiteral("json");
2585:+
2586:+    model->exportChatHistory(filePath, format);
2587:+}
2588:+
2589: #include "moc_UserProfile.cpp"
2590:diff --git a/src/ui/UserProfile.h b/src/ui/UserProfile.h
2591:index b4b73ed..710df04 100644
2592:--- a/src/ui/UserProfile.h
2593:+++ b/src/ui/UserProfile.h
2594:@@ -195,6 +195,7 @@ public:
2595:     Q_INVOKABLE void changeDeviceName(const QString &deviceID, const QString &deviceName);
2596:     Q_INVOKABLE void changeAvatar();
2597:     Q_INVOKABLE void openGlobalProfile();
2598:+    Q_INVOKABLE void downloadChatHistory();
2599: 
2600:     void setIgnored(bool ignored);
2601:     bool ignored() const;
