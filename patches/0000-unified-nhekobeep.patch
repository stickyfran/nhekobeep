diff --git a/resources/qml/MatrixTextField.qml b/resources/qml/MatrixTextField.qml
index 4ceadf6c..e09fb42f 100644
--- a/resources/qml/MatrixTextField.qml
+++ b/resources/qml/MatrixTextField.qml
@@ -80,18 +80,16 @@ ColumnLayout {
                 reversible: true
                 to: "focused"
 
-                NumberAnimation {
+                YAnimator {
                     alwaysRunToEnd: true
                     duration: 210
                     easing.type: Easing.InCubic
-                    properties: "y"
                     target: labelC
                 }
-                NumberAnimation {
+                OpacityAnimator {
                     alwaysRunToEnd: true
                     duration: 210
                     easing.type: Easing.InCubic
-                    properties: "opacity"
                     target: input
                 }
             }
diff --git a/resources/qml/MessageInput.qml b/resources/qml/MessageInput.qml
index 4cc1445c..bcc23135 100644
--- a/resources/qml/MessageInput.qml
+++ b/resources/qml/MessageInput.qml
@@ -345,18 +345,16 @@ Rectangle {
                     y: messageInput.positionToRectangle(messageInput.completerTriggeredAt).y - height
 
                     enter: Transition {
-                        NumberAnimation {
+                        OpacityAnimator {
                             duration: 100
                             from: 0
-                            property: "opacity"
                             to: 1
                         }
                     }
                     exit: Transition {
-                        NumberAnimation {
+                        OpacityAnimator {
                             duration: 100
                             from: 1
-                            property: "opacity"
                             to: 0
                         }
                     }
diff --git a/resources/qml/MessageView.qml b/resources/qml/MessageView.qml
index 84b34a7b..d732a9c4 100644
--- a/resources/qml/MessageView.qml
+++ b/resources/qml/MessageView.qml
@@ -56,8 +56,8 @@ Item {
         anchors.fill: parent
         anchors.rightMargin: scrollbar.interactive ? scrollbar.width : 0
         // reuseItems still has a few bugs, see https://bugreports.qt.io/browse/QTBUG-95105 https://bugreports.qt.io/browse/QTBUG-95107
-        //onModelChanged: if (room) room.sendReset()
-        //reuseItems: true
+        reuseItems: true
+        cacheBuffer: 2000
         boundsBehavior: Flickable.StopAtBounds
         displayMarginBeginning: height / 4
         displayMarginEnd: height / 4
@@ -73,7 +73,10 @@ Item {
             lastScrollPos = (contentY+height);
         }
         onMovementEnded: updateLastScroll()
-        onModelChanged: updateLastScroll()
+        onModelChanged: {
+            updateLastScroll()
+            if (room) room.sendReset()
+        }
         onHeightChanged: contentY = (lastScrollPos-height)
 
         Component {
diff --git a/resources/qml/RoomList.qml b/resources/qml/RoomList.qml
index db4f2cb7..09cf5d71 100644
--- a/resources/qml/RoomList.qml
+++ b/resources/qml/RoomList.qml
@@ -442,7 +442,8 @@ Page {
         model: Rooms
         boundsBehavior: Flickable.StopAtBounds
 
-        //reuseItems: true
+        reuseItems: true
+        cacheBuffer: 1000
         ScrollBar.vertical: ScrollBar {
             id: scrollbar
 
diff --git a/resources/qml/TimelineBubbleMessageStyle.qml b/resources/qml/TimelineBubbleMessageStyle.qml
index 722718bc..0a5a0a5e 100644
--- a/resources/qml/TimelineBubbleMessageStyle.qml
+++ b/resources/qml/TimelineBubbleMessageStyle.qml
@@ -62,7 +62,7 @@ TimelineEvent {
             id: section
 
             active: wrapper.previousMessageUserId !== wrapper.userId || wrapper.showSection || wrapper.previousMessageIsStateEvent !== wrapper.isStateEvent
-            //asynchronous: true
+            asynchronous: true
             sourceComponent: TimelineSectionHeader {
                 day: wrapper.day
                 isSender: wrapper.isSender
@@ -219,7 +219,7 @@ TimelineEvent {
 
                             property color userColor: TimelineManager.userColor(wrapper.reply?.userId ?? '', palette.base)
 
-                            clip: true
+                            clip: false
 
                             NhekoCursorShape {
                                 anchors.fill: parent
diff --git a/resources/qml/TimelineDefaultMessageStyle.qml b/resources/qml/TimelineDefaultMessageStyle.qml
index 49454ac0..ffe78a80 100644
--- a/resources/qml/TimelineDefaultMessageStyle.qml
+++ b/resources/qml/TimelineDefaultMessageStyle.qml
@@ -60,7 +60,7 @@ TimelineEvent {
             id: section
 
             active: wrapper.previousMessageUserId !== wrapper.userId || wrapper.showSection || wrapper.previousMessageIsStateEvent !== wrapper.isStateEvent
-            //asynchronous: true
+            asynchronous: true
             sourceComponent: TimelineSectionHeader {
                 day: wrapper.day
                 isSender: wrapper.isSender
@@ -198,7 +198,7 @@ TimelineEvent {
 
                     property color userColor: TimelineManager.userColor(wrapper.reply?.userId ?? '', palette.base)
 
-                    clip: true
+                    clip: false
 
                     NhekoCursorShape {
                         anchors.fill: parent
diff --git a/resources/qml/ui/Ripple.qml b/resources/qml/ui/Ripple.qml
index 9d871419..e8331432 100644
--- a/resources/qml/ui/Ripple.qml
+++ b/resources/qml/ui/Ripple.qml
@@ -104,11 +104,10 @@ Item {
 
                                 }
 
-                                NumberAnimation {
+                                OpacityAnimator {
                                     id: opacity_animation
 
                                     target: circle
-                                    properties: "opacity"
                                     to: 0
                                     duration: ripple.opacityAnimationDuration
 
diff --git a/src/ChatPage.cpp b/src/ChatPage.cpp
index bc83973a..9e4adc7b 100644
--- a/src/ChatPage.cpp
+++ b/src/ChatPage.cpp
@@ -876,7 +876,7 @@ ChatPage::handleSyncResponse(const mtx::responses::Sync &res, const std::string
     }
 
     if (shouldThrottleSync())
-        QTimer::singleShot(1000, this, &ChatPage::trySyncCb);
+        QTimer::singleShot(10000, this, &ChatPage::trySyncCb);
     else
         emit trySyncCb();
 }
diff --git a/src/main.cpp b/src/main.cpp
index bc13cdca..580aa3b9 100644
--- a/src/main.cpp
+++ b/src/main.cpp
@@ -5,6 +5,7 @@
 #include <iostream>
 
 #include <QApplication>
+#include <QByteArray>
 #include <QCommandLineParser>
 #include <QDesktopServices>
 #include <QDir>
@@ -165,6 +166,14 @@ main(int argc, char *argv[])
         qputenv("QML_DISABLE_DISK_CACHE", "1");
     }
 
+    // Limit QML image cache to 50MB to reduce RAM usage
+    if (qgetenv("QML_IMAGE_CACHE_SIZE").size() == 0) {
+        qputenv("QML_IMAGE_CACHE_SIZE", QByteArray("52428800"));
+    }
+
+    // Force threaded render loop for maximum fluidity (overrides basic if set by default)
+    qputenv("QSG_RENDER_LOOP", QByteArray("threaded"));
+
     // this needs to be after setting the application name. Or how would we find our settings
     // file then?
 #if !defined(Q_OS_MACOS)
diff --git a/src/timeline/EventStore.cpp b/src/timeline/EventStore.cpp
index bc086a14..80b62764 100644
--- a/src/timeline/EventStore.cpp
+++ b/src/timeline/EventStore.cpp
@@ -929,7 +929,7 @@ EventStore::fetchMore()
     mtx::http::MessagesOpts opts;
     opts.room_id = room_id_;
     opts.from    = cache::client()->previousBatchToken(room_id_);
-    opts.limit   = 80;
+    opts.limit   = 40;
 
     nhlog::ui()->debug("Paginating room {}, token {}", opts.room_id, opts.from);
 
